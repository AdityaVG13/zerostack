# INSTALL-FOR-AGENTS -- parked

Installer and `zs` wrapper work is parked while the ZeroStack corpus is being
rearranged. Do not add, revive, or document an install CLI in this tree.

- Removed: `scripts/zs`, `scripts/install_zs.py`, `scripts/install_zerostack.py`
- Kept: in-process runtime (`zsx-core`, `zsx-node`) and the local `zsx` crate
- Do not file beads about installing ZeroStack until the corpus shift settles

If you need a local development binary, build `zsx` from source later. That is
not an install path and is not in scope right now.
