# Scout Pack 01 -- Draft 6 Papers Text (00-04): Additions Beyond Phase A

**Source:** `/tmp/racc_v6/ZeroStack_RACC_Cumulative_V6_Release/current/model_ingest/packs/01_CURRENT_PAPERS_TEXT.md` (83,185 bytes, 1,654 lines -- read sequentially to EOF in 7 chunks, no gaps).
**Contents:** five papers as extracted text:
- 00 Core (pack lines ~9-400), 01 Zero Execute Harness Runtime (~402-640), 02 RACC-Q99 Causal Residency (~642-1000), 03 Implementation and Conformance (~1002-1250), 04 Research Agenda (~1252-1654).

**Cross-check method:** every theorem below was checked against the Phase A corpus in the same release (`current/implementation/*.md`, `current/model_ingest/packs/00_CANONICAL_IMPLEMENTATION_PACK.md` incl. the V6 Theorem-to-Runtime Map rows V6-C01..C10, V6-H01..H05, V6-Q01..Q12, V6-I01, and `current/research/V6_RESEARCH_AGENDA.md`) via targeted grep. ALREADY_IN_PHASE_A = present in those; NO = paper-only; PARTIAL = theorem mapped in Phase A but the paper adds formula/constraint detail absent from Phase A docs.

---

## SUMMARY

Phase A already captured: all V6-C/H/Q/I theorem names + their runtime checkers/falsifiers (Theorem-to-Runtime Map), the 20 Draft-6 ZS-* requirements, Q99 residency/eviction math (canonical spec §6-8), the fault program + public claim gates, phases 0-10, operational targets (0.1% CPU / 500 MB), and a condensed research agenda. **The papers add, beyond that:** (1) four process theorems that are NOT in the V6 map -- Explanation Evidence Preservation, Decision-Delimited Refactor (d+1 calls), Port Nonregression under Complete Observational Coverage, Greenfield Strategy Preservation; (2) the exact formulas behind the mapped theorems (LCP rewrite residual, horizon break-even, call-reduction ratio, 3-term frontier decomposition, Gamma formula, knapsack equivalence, eviction slack); (3) paper-only inventories: trusted boundary (7 untrusted / 11 trusted components), 17-checker list, 9-family semantic object inventory, 17-coordinate raw resource vector, 11-field capability asset record, 8-field continuation handle tuple; (4) the 7-element theorem-to-runtime rule with the "no finite checker = no production authority" gate; (5) conformance conditions lists (no-degradation x7, cache x6, transactional outcomes x3/fail-conditions x6); (6) research agenda content beyond the 50-line `V6_RESEARCH_AGENDA.md` (2 extra proof priorities, 5 experiment groups, 9 kill criteria, workload strata, capability shadow-mode kill conditions, novelty-review scope, 13-item public repository gate); (7) explicit non-claims and proof side conditions per theorem.

No NEW supersessions are introduced by the papers themselves beyond corrections A-H (already Phase A); the one paper-internal correction (Q99 §2: LCP exact characterization replaces "any historical shortening destroys the suffix" overstatement) is itself already mapped as V6-Q01.

---

## OBLIGATIONS

Format: **O# -- Obligation (paper § / theorem). ALREADY_IN_PHASE_A.** Source citation.

### Paper 00 -- Core (`00_ZeroStack_RACC_Cumulative_Core_Draft6.txt`)

1. **O1 -- Protected task order**: declared "no worse than" relation per task binding exact behavior, tests, API compatibility, security, performance, formatting, factual support, human approval, or explicit criterion (Def 1.1). **PARTIAL** -- V6-C08 maps the guarded nonregression use; the definitional enumeration is paper-only.
2. **O2 -- Baseline strategy inclusion**: trace-capable injection ΠB(x) ↪ ΠZ(x); ZeroStack may add strategies, never remove the baseline path (Def 1.2, §1). **YES** -- V6-C07, ZS-METRIC-011.
3. **O3 -- Protected decision equivalence + protected-sufficient decision view**: vi(o) = vi(o′) ⇒ o ∼i o′; exact expansion authority before authority is exercised (Def 2.1, 2.2). **YES** -- V6-C01, ZS-VIEW-010.
4. **O4 -- Decision-view minimality**: quotient Oi/∼i is the smallest exact alphabet up to relabeling; every exact view refines the quotient (Thm 2.3). **YES** -- V6-C01 (map adds falsifier: "two observations merged by a certified view require different protected decisions or expansion rules").
5. **O5 -- Privately composable segment**: 6 clauses -- same rooted start; all internal branches mechanical/verifiable/covered by contingent policy; no unauthorized authoritative effect; final view protected-sufficient; exact expansion + baseline escape remain; failure returns Unknown, a counterexample, or the baseline path (Def 3.1). **PARTIAL** -- V6-C02 + ZS-EXEC-001/002/007 cover clauses 1-5; clause 6 (failure taxonomy: Unknown/counterexample/baseline) is paper-only.
6. **O6 -- Harness decision-boundary compression**: replacing each privately composable σi by one macro-transition preserves protected decision opportunities and final protected outcome; call reduction = 1 − m/K (Thm 3.2). **PARTIAL** -- theorem mapped (V6-C02); the explicit reduction ratio and "hidden backend work is real and charged separately" (§3) are paper-only.
7. **O7 -- Unresolved adaptive decision**: future observation can require two protected-distinct actions with no unique selection by request/policy/verifier (Def 4.1). **YES** -- V6-C03, ZS-EXEC-007.
8. **O8 -- Adaptive decision-round lower bound**: protocol preserving all baseline strategies must return to the model at least once per unresolved adaptive decision (Thm 4.2). **YES** -- V6-C03.
9. **O9 -- One-/two-call normal form**: N_Z(x) = D_Z(x) + 1 when all other work is privately composable (Cor 4.3). **YES** -- V6-C04, ZS-BENCH-010.
10. **O10 -- Snap-to-edit sufficiency**: no primitive discovery call needed if the completeness-certified indexed lens contains E*(q,s,π) ∪ W*(q,s,π); unresolved π may present the protected architecture decision instead (Thm 5.1). **YES** -- V6-C05, ZS-GRAPH-005, ZS-VIEW-010.
11. **O11 -- Stable reference**: opaque harness-visible handle binds exact project object, contract, root, expansion authority; "the handle is not commit authority" (Def 5.2). **YES** -- V6-C06, ZS-SESSION-001/005 (ABI).
12. **O12 -- Stable-reference recovery**: handle resolution under the same rooted contract preserves exact recoverability; recovery = stable reference + incremental decision, not lossy summary (Thm 5.3). **YES** -- V6-C06.
13. **O13 -- Same-harness capability superset**: ΠB ↪ ΠZ and sup over ΠZ ≥ sup over ΠB; "capability theorem, not a controller theorem -- the system must still prevent a controller from selecting a worse added strategy" (Thm 6.1). **PARTIAL** -- V6-C07 mapped; the controller caveat is paper-only.
14. **O14 -- Guarded protected nonregression**: publish zx only with sound certificate bx ⪯x zx and, for stateful work, successor in the protected future-simulation relation; otherwise publish/restore bx; D(x) satisfies bx ⪯x D(x); unsupported dimensions stay Unknown (Thm 6.2). **YES** -- V6-C08, ZS-VERIFY-001/006.
15. **O15 -- Reasoning sovereignty**: max reasoning allowance, tool access, evidence expansion, stopping freedom no weaker than baseline; nonzero all-token floor if hidden reasoning is positive and charged; "preserving reasoning tokens and simultaneously claiming those same tokens were eliminated is inconsistent" (Def 7.1). **PARTIAL** -- ZS-METRIC-011 audits settings; the all-token-floor consistency constraint is paper-only.
16. **O16 -- Frontier closure**: C_n = P_n/n + (1−νn)Hn + νnFn; S_n = 1 − C_n/B_n; S_n → 1 iff each of the three normalized terms → 0 (Thm 8.1). **PARTIAL** -- V6-C09 mapped (falsifier: "savings approach one while a normalized burden stays positive"); explicit formulas paper-only.
17. **O17 -- Certified redundant-work closure / 100% endpoint**: Γ = (B−Z)/(B−L) with sound L ≤ Z ≤ B, B > L; Γ = 1 ⇔ Z = L; L includes request info, genuine decisions, protected reasoning, verification, novel output, external effects (Def 8.2, Thm 8.3). **PARTIAL** -- V6-C10 + ZS-METRIC-010 mapped; the Gamma formula is paper-only.
18. **O18 -- Forty-token illustration**: 1 − 40/1,000,000 = 99.996% (one exchange), 99.992% (two); hidden backend work excluded unless added to the same resource coordinate (paper §9). **NO** -- paper-only worked example; relevant to ZS-BENCH-010-style reporting and to the "model-visible transcript can approach the irreducible decision boundary" claim.

### Paper 01 -- Zero Execute Harness Runtime (`01_Zero_Execute_Harness_Runtime_Draft6.txt`)

19. **O19 -- Six-way result taxonomy**: Completed, DecisionRequired, EvidenceExpansionRequired, VerificationUnknown, BaselineFallbackRequired, RejectedNoMutation; "an incomplete verifier or missing causal edge does not become a vague success response" (§1). **YES** -- taxonomy present in canonical pack (result grammar line ~350); the epistemic-limit sentence is paper framing.
20. **O20 -- Request binding set**: task+protected criteria, exact root, optional continuation, operation class+objective, model decision/contingent policy, side-effect policy, model/harness/tool/reasoning/verification contracts, private-composition+resource budgets, baseline reserve+fallback policy (§1). **YES** -- ZERO_EXECUTE_SEMANTIC_ABI_V6.md.
21. **O21 -- Faithful adapter**: Da(Ra(s)) = s and preserves cancellation, timeout, Unknown, baseline escape, result ordering, resource-accounting semantics (Def 2.1). **PARTIAL** -- V6-H01 + ZS-ADAPTER-009/011 mapped; the 6 preserved semantics are paper-only (ABI mentions faithful adapter but not this enumeration).
22. **O22 -- Transport factorization**: faithful adapters over the same rooted backend transition give results equivalent up to declared rendering differences (Thm 2.2). **YES** -- V6-H01; cross-harness list (Pi, Codex CLI, Claude CLI, Cursor, Grok Build, RPC, CLI/stdio, MCP) already in canonical pack.
23. **O23 -- Continuation handle tuple**: κt = H(task, project, evidence, candidate, verification, ledger, contracts, epoch) -- 8 bound roots (§3). **PARTIAL** -- ZS-SESSION-005 binds ABI/task/project/security/model-harness rendering/epoch roots; the exact 8-field composition is paper-only.
24. **O24 -- Continuation sufficiency + fail-closed set**: handle + new decision reproduces every protected transition; fail closed on stale roots, incompatible ABI, cross-project scope, forged identity, revoked epoch; handles may survive restart and compatible harness changes (Thm 3.1). **PARTIAL** -- V6-H02 mapped; fail-closed enumeration paper-only.
25. **O25 -- Private composition eligibility**: all internal choices deterministic/verifier-resolved/contingent-policy-covered; no intermediate observation needed for an uncovered decision; effects sandboxed or separately authorized; final view protected-sufficient (Def 4.1). **YES** -- V6-H03, ZS-EXEC-002/007.
26. **O26 -- Decision-preserving composition + runtime rule**: backend MUST return DecisionRequired when eligibility ceases ("runtime manifestation of the adaptive decision-round lower bound") (Thm 4.2). **YES** -- V6-H03 + ZS-EXEC-007.
27. **O27 -- Explanation process**: 6-step pipeline (bind request+factual-support criteria; exact snapshot; resolve entry points/modules/config/data-control paths/tests/runtime evidence; task-relative evidence graph; compact view with exact rooted references; expand disputed factual claims before publication) + **Explanation Evidence Preservation** (Thm 5.1) -- compact interface does not reduce baseline factual strategy set. **NO** -- process theorems absent from ABI and V6 map.
28. **O28 -- Refactor contract**: binds objective+architectural constraints, public API/behavior preservation, build/test/security/performance criteria, allowed side effects, subjective/unverifiable dimensions, baseline fallback; backend resolves affected closure, returns only genuine architecture choices, stages typed/preimage-bound effects in child sandbox, derives complete delta, verifies result+successor, atomically commits or restores; **Decision-Delimited Refactor** (Thm 6.1): exactly d+1 calls for d unresolved adaptive decisions (rename d=0; one API design choice d=1). **NO** -- not in ABI/map; it is the operational instance of Cor 4.3 (V6-C04) but with its own checker obligation.
29. **O29 -- Cross-language porting**: 7 observable-obligation categories (inputs/outputs/errors; state transitions+side effects; serialization+persistent formats; numerical behavior+tolerances; ordering/concurrency/timing; API/CLI compat; platform/build/package/performance/memory); **Port Nonregression under Complete Observational Coverage** (Thm 7.1): V = B + sound verifier + target satisfies all + source baseline available for uncovered environment cases ⇒ protected-equivalent within the declared contract; V ≠ B ⇒ unverified obligations remain Unknown, milestone allowed but complete equivalence may not be claimed. **NO** -- not in ABI/map.
30. **O30 -- Greenfield construction**: preserves milestone roots+accepted design decisions, scene/entity/module structure, build+platform state, deterministic tests+performance budgets, exact rollback+branch history, human authority over subjective quality; **Greenfield Strategy Preservation** (Thm 8.1): optional suggestions + expandable evidence + native tools + subjective decisions with model/user ⇒ no baseline strategy removed; "quality improvement ... measured by strict rescues, not assumed from lower token counts". **NO** -- not in ABI/map.
31. **O31 -- Atomic no-mutation failure**: candidate effects in child sandbox ŝ; commit requires valid lease binding (parent root s, current epoch, exact delta Δ, verifier + successor receipts); publication via expected-parent CAS; every failed/stale/raced/canceled/rejected attempt leaves authoritative root = s (Thm 9.1). **PARTIAL** -- V6-H04, ZS-KERNEL-008, ZS-STORE-006 mapped; lease-binding tuple paper-only.
32. **O32 -- Crash recovery**: replay append-only event log to exactly old or new root, never a partial mixture (§9). **PARTIAL** -- implied by V6-H04 falsifier; the log-replay statement is paper-only (canonical pack says "CAS publishes the complete successor or nothing").
33. **O33 -- Verified capability compounding**: asset records scope, preconditions, reads, writes, effects, postconditions, verifier, successor relation, rollback, dependencies, freshness, maintenance cost (11 fields); A_n ⊆ A_{n+1}, selector publishes only protected-safe candidates ⇒ optimal protected utility nondecreasing, min complete cost nonincreasing while fresh; "does not justify uncontrolled self-modification"; invalidation can remove assets so maintenance/drift must be measured (Thm 10.1). **PARTIAL** -- V6-H05, ZS-CAP-001/006 mapped; the 11-field asset record is paper-only.
34. **O34 -- Operational architecture**: no spawn-per-call overhead, no mixed worker generations; stable payloads vs volatile receipts separated; workers pinned by exact digests + matched manifests; default idle ~negligible; heavier indexing scheduled/measured/interruptible; errors typed and actionable; **no silent fallback** -- fallback is an explicit result with a complete ledger (§11). **PARTIAL** -- no-spawn + idle budget in Phase A (canonical spec line ~369, ZS-OPS-006); digest pinning/matched manifests/typed errors/ledger-on-fallback paper-only.

### Paper 02 -- RACC-Q99 Causal Residency (`02_RACC_Q99_Causal_Residency_Draft6.txt`)

35. **O35 -- Three-layer miss semantics**: L1 miss = compact prefix processed again; L3 miss = fetch or deterministically rematerialize valid L2 object; L2 miss/invalidity = recompute or reverify project knowledge (§1). **YES** -- ZS-CACHE-013.
36. **O36 -- Exact retrospective rewrite characterization**: lcp(H, H′) = |A| + r; rewritten residual = s + b − r; self-induced prefix break exactly when r < s + b (Thm 2.1). **PARTIAL** -- V6-Q01 mapped; formula + proof-side-condition (finite exact sequences) paper-only (Q99 spec has no LCP math).
37. **O37 -- Cache-compaction crossover**: compaction cheaper iff ρr + u(s+b−r) < ρ(n+b) -- "a small early rewrite converts a large cached residual into uncached processing" (Thm 2.2). **PARTIAL** -- V6-Q02; formula paper-only.
38. **O38 -- Horizon-aware break-even**: Ckeep(T) = T·cr(n+b); Crewrite(T) = cc + (cw−cr)(s+b−r) + T·cr(s+b); rewrite wins iff cc + (cw−cr)(s+b−r) < T·cr(n−s) -- router must estimate destroyed suffix, price ratios, compaction work, reuse horizon (Thm 2.3). **PARTIAL** -- V6-Q03; formulas paper-only.
39. **O39 -- CCNF**: X ↦ (KX, FX, PX, CX, EX) -- complete causal key, formation receipt, immutable payload/root, stable model-visible capsule, exact expansion authority; capsule emitted BEFORE raw tool output enters model-visible history; expansion appends evidence, never rewrites the capsule (Def 3.1). **YES** -- V6-Q04, ZS-VIEW-005.
40. **O40 -- First-emission prefix immunity + zero self-rewrite component**: append-only ⇒ Ht exact prefix of all later histories; miss decomposition into self-rewrite / provider residency-routing / contract changes / new suffix work has self-rewrite component zero (Thm 3.2, Cor 3.3). **PARTIAL** -- V6-Q04 mapped; the 4-way decomposition paper-only.
41. **O41 -- Causal key composition + Causal Cache Soundness**: Kv = H(constructor, contract, dependency roots, canonical parameters, relevant environment); receipt proves payload actually formed under the key -- "a bare key lookup is insufficient" (Thm 4.1). **PARTIAL** -- V6-Q05 mapped; key-hash composition paper-only.
42. **O42 -- Dependency-complete invalidation**: objects outside Desc*(Δ) keep key+value; hard premise = graph completeness; discovered undeclared influence revokes dependent certificates and becomes a refinement counterexample (Thm 5.1). **YES** -- V6-Q06, ZS-GRAPH-006; undeclared-influence rule also in canonical spec.
43. **O43 -- Equality-boundary early cutoff**: complete separating boundary with all boundary roots unchanged ⇒ downstream deterministic values unchanged; preserves cache across formatting-only changes and behavior-preserving refactors (Thm 5.2). **YES** -- V6-Q07, ZS-GRAPH-007.
44. **O44 -- Weighted causal Q99**: R_L2(q) ≥ 0.99 iff invalid demanded weight ≤ 1% of total demanded weight (Thm 6.1). **YES** -- V6-Q08, ZS-CACHE-010.
45. **O45 -- High-impact novelty impossibility**: unresolved invalid demanded mass > 1% with no independent equality/replacement proof ⇒ no L2 Q99 claim until recomputation/verification reduces it; public service metrics should use sliding windows (Cor 6.2). **PARTIAL** -- corollary not mapped separately; sliding-window guidance paper-only.
46. **O46 -- Causal Residency Budget**: tier satisfies physical Q99 iff ∃r: Σ s_i·r_i ≤ C and Σ w_i·r_i ≥ 0.99W; equivalent to 0-1 knapsack optimum ≥ 0.99W; heuristic may propose, "a simple independent checker verifies the capacity and threshold inequalities; optimization and authority remain separate" (Thm 7.1). **YES** -- V6-Q09, ZS-CACHE-011, canonical spec §6.
47. **O47 -- Uniform-size residency**: retain k largest weights; smallest Q99 cardinality = shortest descending-weight prefix reaching 0.99W (Cor 7.2). **YES** -- Q99 implementation spec (line ~97).
48. **O48 -- Q99 eviction slack**: σ = W_R − 0.99W; eviction preserves the certificate iff w(E∩R) ≤ σ; beyond σ needs compensating admissions or new demand certificate; "recency may propose eviction; current demand mass authorizes it" (Thm 8.1). **YES** -- V6-Q10, ZS-CACHE-012, canonical spec §7.
49. **O49 -- Provider residency transposition**: provider hit requires H_P = I ∩ M ∩ R_P; ZeroStack logical hit requires I + storage/authorization only; **Provider-Miss Insulation** (Thm 9.1): L1 miss never requires rediscovering unchanged project state. **YES** -- V6-Q11, ZS-CACHE-013.
50. **O50 -- Provider-Miss Bounded Amplification**: model-visible burden after L1 miss ≤ C + L not B; reduction 1 − (C+L)/B; "backend fetch, rematerialization, verification, and storage are not included in this ratio and must be reported separately" (Thm 9.2). **YES** -- V6-Q12, ZS-CACHE-014.
51. **O51 -- Cross-session/branch/harness reuse + branch convergence**: content roots are semantic identity; branch labels/session IDs/display paths are not; harness rendering is a derived object keyed by rendering contract; authorization stays project/tenant-scoped even when content deduplicates; **Branch Convergence Reuse** (Prop 10.1): identical exact roots at a complete dependency boundary ⇒ downstream L2 objects shareable (Prop 10.1). **PARTIAL** -- ZS-CACHE-015 + ZS-SEC-005 cover the dedup/authorization split; the proposition is paper-only.
52. **O52 -- Q99 reporting discipline**: 9 required coordinates (named layer+resource coordinate; demand set/weights/window; validity+Unknown rules; physical tier+capacity; provider observations for L1; invalidated+recomputed mass; time/work to restore Q99; complete backend cost; confidence/exactness status); "99 percent cached" without these is scientifically underdetermined (§11). **PARTIAL** -- Q99 reporting in canonical spec + ZS-CACHE-010; the 9-field enumeration paper-only.

### Paper 03 -- Implementation and Conformance (`03_ZeroStack_Implementation_Conformance_Draft6.txt`)

53. **O53 -- Seven-element theorem-to-runtime rule**: every release-level theorem compiles into (1) rooted evidence, (2) total deterministic terminating checker, (3) certificate/counterexample machine-revalidatable, (4) authority consequence = only permitted execution/publication implication, (5) Unknown behavior = expansion/refinement/baseline fallback, (6) checker work included in the ledger, (7) direct falsifier test/fault that must cause rejection; **"a theorem that cannot yet supply a finite checker remains a proof target or empirical claim; it cannot authorize production behavior"** (§1). **NO** -- paper-only formulation (canonical pack echoes "no claims from algebra alone" but not the 7-element rule).
54. **O54 -- Trusted boundary**: untrusted = model, planner/controller, retrievers/graph builders, cache optimizer/eviction policy, proof/test generators, capability extractor, harness adapter input; trusted = canonical serialization+root verification, formation-receipt verification, exact snapshot/object resolution, total Safe/Unsafe/Unknown checkers, baseline reserve+restoration, sandbox isolation+exact-delta derivation, verification+successor check, short-lived authority issuance, expected-parent atomic commit, audit replay+ledger sealing; "the model and planner cannot construct authority objects" (§2). **NO** -- paper-only (Phase A has only "module/type boundary preventing untrusted authority construction").
55. **O55 -- Four-repository ownership split**: ZeroStack = semantic ABI/task contract/adapters/orchestration/continuation/authority/verification coordination/capability registry/accounting/conformance; FSZero = immutable bytes/exact objects/snapshots/spans/sandboxes/effect traces/deltas/rollback/materialization/CAS publication, never relevance or rendering; GraphZero = definitions/references/callers/tests/dependency graphs/causal lenses/completeness grades/incremental invalidation, never file mutation or model messages; TokenZero = stable tool schemas/canonical rendering/decision capsules/expansion references/provider-cache observations/token ledgers, never filesystem or graph truth; no domain engine imports another (§3). **YES** -- mirrors hub AGENTS.md law + master plan; the per-engine "does not decide X / never Y" clauses are the paper's added clarity.
56. **O56 -- Concrete runtime refinement**: conforming step = (1) non-authoritative stutter with α(c)=α(c′), (2) permitted abstract evidence-refinement transition, (3) valid certificate for a permitted authoritative abstract transition, or (4) failure leaving authority unchanged/restoring related baseline; plus crash-recovery preservation and output/ledger projection ⇒ every concrete trace projects to a valid abstract RACC trace (Def 4.1, Thm 4.2). **PARTIAL** -- V6-I01 mapped (falsifier: "concrete authoritative trace has no valid abstract projection"); the 4-case conforming-step definition paper-only.
57. **O57 -- Semantic object inventory**: 9 families with required meaning -- Identity/contracts; Project state; Harness state (request envelope, continuation handle, decision view, stable capsule, expansion handle, action table); Execution (sandbox root, plan, effect/read/write trace, exact delta, verifier+successor receipts); Authority (baseline authority, execution lease, commit lease, no-mutation+commit receipts); Cache (causal key, formation receipt, logical validity, physical residency, provider observation, admission/eviction certificate); Accounting (raw resource vector, Q99 report, Pareto interval, frontier-closure report); Capabilities (verified episode, asset, failure syndrome, freshness/revocation receipt); **"semantic identity may not be represented by timestamps, display paths, random row IDs, or mutable object addresses"** (§5). **NO** -- paper-only.
58. **O58 -- Required checkers**: 17 named checkers (canonical-byte+root; formation receipt; task/contract compatibility; decision-view sufficiency for finite/domain scope; continuation compatibility; private-composability/decision-gate; dependency-closure+completeness; equality-boundary cutoff; L2 logical validity; L3 capacity/Q99 residency; eviction slack; sandbox/effect-scope; verifier+future-successor; authority lease; expected-parent CAS; complete-resource+Pareto; capability freshness/revocation); "optimizer may be sophisticated/learned/heuristic; the checker should be small, deterministic, independently testable" (§6, referencing the 130-requirement backlog). **NO** -- canonical pack names only a few of these individually.
59. **O59 -- Implementation sequence**: 7.1 audit+baseline (map requirements to code, freeze same-model/same-harness traces); 7.2 identity+epistemic kernel (canonical serialization, roots, receipts, versioned contracts, append-only event log, Safe/Unsafe/Unknown, prohibit ordinary code from constructing authority); 7.3 read-only Zero Execute = first product milestone; 7.4 cache factorization+invalidation; 7.5 private composition (measure critical path + transcript reduction); 7.6 transactional effects; 7.7 residency+capabilities with "learning is last because it depends on every preceding truth and authority layer" (§7). **YES** -- matches canonical pack phases 0-10.
60. **O60 -- No-degradation conformance conditions**: 7 conditions (rooted baseline model/harness/tools/reasoning/stopping; native tools+full baseline path callable; optimization cannot consume fallback reserve; every hidden operation decision-preserving; every publication equivalent/dominating or baseline; stateful commits preserve future protected simulation; Unknown dimensions disclosed); **"an accuracy benchmark alone cannot prove strategy inclusion; conformance tests must deliberately request baseline escape, exact expansion, and native tools after optimized operation"** (§8). **NO** -- the 7-condition list and the deliberate-escape test design are paper-only.
61. **O61 -- Cache conformance**: causal-cache hit requires canonical key equality, valid formation receipt, exact payload/root integrity, current dependency/contract validity, authorized scope, correct layer classification; Q99 certificate additionally requires exact denominator/weights/window/tier/capacity and post-eviction state; "provider cached tokens are observed, not inferred from L2" (§9). **PARTIAL** -- pieces exist in ZS-CACHE-* requirements; the 6-condition hit gate and the observe-not-infer rule are paper-only.
62. **O62 -- Transactional conformance**: fault program injects crashes/races before and after every verify/authorize/commit boundary; accepted outcomes = complete verified successor root | exact old root with no-mutation receipt | exact baseline restoration; fail conditions = partial mutation, mixed worker generations, silent fallback, stale-lease commit, missing ledger entries (§10). **PARTIAL** -- fault program in canonical pack §8; the accepted-outcome set is paper-only.
63. **O63 -- Complete accounting**: raw resource vector ≥ 17 coordinates (model calls, arguments, results, uncached input, cache reads, cache writes, reasoning, output, wire bytes, disk bytes, CPU, GPU, indexing, verification, latency, storage, failed work, fallback); scalar cost only with declared nonnegative weights; raw coordinates remain visible; preparation/maintenance amortized over a declared campaign, never omitted (§11). **NO** -- raw vector not present in Phase A implementation docs.
64. **O64 -- Fault catalog**: mandatory attacks = noncanonical encodings, payload/key substitution, missing dependency edges, stale roots, forged/replayed leases, cross-project handles, provider historical rewrites, L2 corruption, L3 loss, sandbox escape, undeclared effects, verifier disagreement, CAS races, fallback reserve exhaustion, stale capabilities, adapter truncation, ledger omission; expected response Unsafe/Unknown/exact no-mutation/baseline fallback; **"a successful optimized publication under one of these faults is a release blocker"** (§12). **PARTIAL** -- canonical pack §8 has a 19-item superset fault list + expected responses; the release-blocker rule is paper-only.
65. **O65 -- Operational gates**: no heavy daemon; ≈≤0.1% background CPU and ≤500 MB resident memory default local mode; heavier indexing scheduled+disclosed; no spawn-per-call, no random worker checkout, no mixed-generation rollback; workers/manifests pinned by exact digests (§13). **YES** -- ZS-OPS-006 + canonical spec.
66. **O66 -- Public empirical gate**: publish same-model/same-harness paired traces; one-/two-call decision annotations; protected regression + strict-rescue adjudication; forced L1/L2/L3 miss matrix; causal invalidation + Q99 restoration curves; full resource vectors; storage+daemon overhead; fault-injection results; negative findings + unsupported dimensions; implementation commit/schemas/manifests/checksums (§14). **PARTIAL** -- canonical pack §9 "public claim gates" overlaps partially; trace/annotation/curves/commit-schema items paper-only.

### Paper 04 -- Research Agenda (`04_ZeroStack_RACC_Draft6_Research_Agenda.txt`)

67. **O67 -- Formal proof program**: 10 priorities (finite harness decision-boundary trace projection; adaptive decision-round lower bound; exact LCP rewrite + horizon economics; dependency-complete DAG invalidation + equality-boundary cutoff; Q99 residency budget + eviction slack; multi-resource Pareto interval intersection; atomic runtime refinement + no-partial-authority; protected successor simulation; finite decision-view quotient + exact expansion; certified redundant-work lower-bound composition); **Lean formalization on explicit finite structures first; general symbolic/open-world claims remain conditional on domain verifiers and receipts** (§1). **PARTIAL** -- `V6_RESEARCH_AGENDA.md` has 8 of 10 (omits adaptive lower bound and certified lower-bound composition) + no Lean instruction.
68. **O68 -- Implementation experiments**: 5 groups -- read-only Zero Execute minimal path (measure factual support + transcript reduction before any write path); causal graph completeness (inject undeclared dependencies; measure completeness grades, Unknown rate, refinement convergence, false-safe rate; **single stale accepted object from a missing edge = release blocker for claimed scope**); cache layers (force L1 miss/L2 warm-L3 cold/L2 invalidity/storage corruption/branch divergence+convergence; report replay, backend work, transfer, recomputation, Q99 restoration separately); decision depth (blind annotation of real baseline traces; **publish tasks where one-/two-call normal form fails**); transactional effects (inject failures at every instruction boundary around sandbox/verification/lease issuance/CAS) (§2). **NO** -- not in `V6_RESEARCH_AGENDA.md` or canonical pack.
69. **O69 -- Workload strata**: 9 strata -- repository explanation, local refactor, cross-cutting refactor, API migration, Python-to-C++ port, dependency/build migration, security repair, performance work, greenfield game/application; **study mature repetitive repos separately from novel greenfield; novelty/fallback fraction expected to differ substantially** (§3). **PARTIAL** -- canonical pack benchmark program has refactor/greenfield task lists; the full 9-stratum + separation rule paper-only.
70. **O70 -- Q99 research measures**: 10 measures (weighted logical L2 reuse; local+remote L3 residency; provider L1 hits; model-visible project-context elimination; complete-work reduction; capacity for L3 Q99; eviction-slack consumption; time/work to restore Q99; equality-cutoff frequency; invalidation hazard by object class); compare LRU/LFU vs size-, causal-value-, hazard-, recomputation-aware plans; **optimizer cost and checker cost remain separate** (§4). **PARTIAL** -- canonical pack §7 lists the plan comparisons; the 10-measure set paper-only.
71. **O71 -- No-degradation evaluation**: 8 measures -- protected regressions; strict rescues; factual evidence support; build/test/security outcomes; reasoning allowance + native-tool availability; subjective dimensions via blinded human review; fallback reserve + deadline success; long-horizon successor effects; **no universal semantic claim outside the protected verifier/human scope** (§5). **PARTIAL** -- strict rescue/regression in canonical pack; blinded review, deadline success, long-horizon effects paper-only.
72. **O72 -- Operational research**: idle+active CPU, memory, index update latency, storage growth, compaction, corruption recovery, multi-project isolation; heavier whole-repository analysis scheduled, cancelable, attributed to task/campaign ledger (§6). **PARTIAL** -- ZS-OPS-006 covers idle targets; the measure set paper-only.
73. **O73 -- Capability research**: capture rate, proof cost, applicability, invalidation hazard, maintenance, strict rescues, lifetime value; **capabilities begin in shadow mode; negative transfer, stale execution, and cross-project leakage are kill conditions** (§7). **NO** -- shadow-mode and kill conditions not in Phase A.
74. **O74 -- Novelty and literature review**: backward/forward citations across communication complexity, zero-error information, incremental computation, build systems, content-addressed storage, proof-carrying code, runtime assurance, program analysis, agent interfaces, prompt caching, software-engineering agents; **candidate originality = exact composition, not ownership of established components; historical priority unresolved until independent review** (§8). **NO** -- paper-only.
75. **O75 -- Nine kill criteria**: reject/rescope when -- (1) token reduction hides a genuine semantic decision; (2) causal incompleteness causes a protected stale hit; (3) fallback cannot run within baseline budget/deadline; (4) complete work exceeds baseline without declared quality trade; (5) Q99 omits misses/Unknown/maintenance/invalidation; (6) provider caching already captures the saving at lower complete cost; (7) capability capture/maintenance has nonpositive lifetime value; (8) persistent backend violates operational overhead constraints; (9) same-model treatment shows protected regressions without exact fallback (§9). **NO** -- paper-only (not in `V6_RESEARCH_AGENDA.md`).
76. **O76 -- Research stop rule**: no new headline theorem enters the current release unless it changes a runtime authority decision, tightens a certified lower bound, yields a finite checker, reduces a measured Frontier Closure burden, explains a reproducible failure, or proves an impossibility that prevents wasted engineering (§10). **YES** -- in `V6_RESEARCH_AGENDA.md`.
77. **O77 -- Public repository gate**: release contains papers, sources, claim status, correction ledger, implementation requirements, schemas, traces, raw ledgers, benchmark scripts, negative results, provider facts with dates, manifests, hashes, reproduction instructions; **papers remain labeled Draft until independent mathematical and systems review** (§11). **PARTIAL** -- canonical §9 claim gates overlap; repo-content composition + Draft labeling paper-only.
78. **O78 -- Agenda status discipline**: "items are not represented as achieved"; agenda evolves while the four Draft 6 papers remain stable research objects; papers remain archival theorem conclusions only for the four (research status block). **YES** -- `V6_RESEARCH_AGENDA.md` first line.

---

## SUPERSESSIONS

No new supersessions beyond corrections A-H (already Phase A). Paper-internal statements confirming prior corrections, for the record:

1. **Q99 §2** (Thm 2.1): "The theorem corrects the overly broad claim that every historical shortening necessarily destroys the entire later suffix. It frequently does, but the exact common-prefix length controls the result." -- already mapped as V6-Q01; no change.
2. **Execute research status**: "refines the architectural correction introduced in Draft 3" (harness-side runtime, not model-integrated; no modified weights/tokenizer/KV state/inference kernel; MCP is one adapter, not the architecture) -- already Phase A.
3. **Core research status**: Draft 6 consolidates Draft 3 harness correction + Drafts 4/5 cache-stability/causal-invalidation + inherited no-degradation/Pareto results -- aligns with canonical pack Phase A/B/C authority ordering.
4. **Conf §1**: a theorem without a finite checker "cannot authorize production behavior" -- this sharpens the Phase A "never claim from algebra alone" rule (canonical pack) into a per-theorem release gate; treat as an addition (O53), not a supersession.
5. **Agenda §11**: "Papers remain labeled Draft until independent mathematical and systems review" -- publication state, consistent with corrections A-H.

**Watch item (potential future supersession):** the 4 process theorems (O27, O28, O29, O30) exist only in paper 01 and have NO rows in the V6 Theorem-to-Runtime Map. Any implementation claiming these theorems needs new map rows + checkers (e.g., refactor d+1 call-count checker; port V=B coverage checker; explanation factual-support checker) -- the missing evidence register should record them as unmapped.

---

## NOTABLE

**Proof side conditions / conditional-claim premises (for each theorem, from the papers' own statements):**
- Core: production claims conditional on 7 elements -- rooted project identity, protected decision-view sufficiency, exact expansion, baseline strategy inclusion, verifier soundness, future-safe successor publication, complete accounting (research status).
- Core Thm 3.2: induction relies on "same rooted state" at every boundary + unauthorized effects excluded by premise; Thm 6.2 is "scope-bound" -- unsupported semantic dimensions remain Unknown and cannot acquire publication authority.
- Execute Thm 7.1: V = B (complete observational coverage) is required for the equivalence claim; V ≠ B ⇒ Unknown, milestone-only. Thm 5.1: "does not guarantee that the model writes the best explanation; it guarantees that compression does not remove the evidence authority needed to match the baseline."
- Q99: runtime claims conditional on canonical identity, complete causal keys, formation receipts, dependency completeness, storage integrity, correct demand denominators, independent checker authority (research status). Thm 4.1: "a bare key lookup is insufficient" -- receipt required. Thm 5.1: "the hard premise is graph completeness." Thm 6.1: positive weights wi > 0 demanded closure; denominators include misses and Unknown objects (ZS-CACHE-010).
- Q99 §1: documented provider volatility -- OpenAI exact prefix reuse + append-only/stable-tool-ordering recommendation; Anthropic 5-minute and 1-hour cache durations; xAI misses after eviction + cached-token observations; Google implicit/explicit caching with explicit-cache lifetime controls. None is treated as sole authority for durable state.
- Conf research status: "conformance remains unestablished until the concrete runtime is audited, fault-injected, benchmarked against the same-model/same-harness baseline, and shown to refine the abstract state machine"; "until the actual Rust events and authority paths satisfy [Thm 4.2], the papers do not prove the implementation." O53's rule applies retroactively to every mapped theorem.

**Explicit non-claims:**
- Core §10: "a conservation result rather than a magic compression claim"; one-/two-call is about decision depth and recoverable state, "not about hiding the world from the model." Historical novelty explicitly unresolved.
- Core §7: nonzero all-token floor; cannot preserve reasoning tokens and simultaneously claim they were eliminated.
- Execute: model unchanged; backend may not "silently choose between protected-distinct semantic outcomes and call that compression" (Core §1); capability theorem is not a controller theorem (Thm 6.1); no uncontrolled self-modification (Thm 10.1).
- Q99: "Q99 is never a universal claim: every result names its cache layer, resource coordinate, workload, weights, and measurement window"; "99 percent cached" without coordinates is scientifically underdetermined (§11); provider hit requires I ∩ M ∩ R_P simultaneously (Thm 9.1 area).
- Agenda: items "not represented as achieved"; quality improvement "must be measured by strict rescues, not assumed from lower token counts" (Execute §8).

**Notable details for downstream design:**
- Result taxonomy includes RejectedNoMutation (a no-mutation failure surfaced as a first-class result, not an exception).
- Handle fail-closed set (O24) is the operational spec for ZS-SESSION-005's "resolve through explicit migration or fail closed".
- The 99.996% figure (O18) must be reported with the "hidden backend work charged separately" caveat -- candidate for the benchmark/claim-guidance doc.
- Conf §6's "optimizer may be sophisticated, learned, or heuristic; checker small, deterministic, independently testable" is the core optimizer/checker separation that also appears in Q99 §7 (residency plan proposal vs authority) and Agenda §4 (optimizer cost vs checker cost).
- Capability assets in shadow mode first (O73) -- aligns with ZS-CAP-006 but adds an operational staging rule not in the backlog.

---

## COVERAGE_CONFIRMATION

- **Read coverage:** full pack read sequentially in 7 chunks (lines 1-250, 251-500, 501-750, 751-1000, 1001-1250, 1251-1500, 1501-1654); EOF reached; no lines skipped. All five papers extracted.
- **Theorem coverage:** all theorems/lemmas/corollaries/propositions in the papers are accounted for: Core Thm 2.3, 3.2, 4.2, Cor 4.3, Thm 5.1, 5.3, 6.1, 6.2, 8.1, 8.3 (10); Execute Thm 2.2, 3.1, 4.2, 5.1, 6.1, 7.1, 8.1, 9.1, 10.1 (9); Q99 Thm 2.1, 2.2, 2.3, 3.2, Cor 3.3, Thm 4.1, 5.1, 5.2, 6.1, Cor 6.2, Thm 7.1, Cor 7.2, Thm 8.1, 9.1, 9.2, Prop 10.1 (16); Conformance Thm 4.2 (1); no lemmas marked as such anywhere. All mapped to V6 rows where they exist; 4 process theorems unmapped (O27-O30) -- flagged.
- **Definitions covered:** Def 1.1, 1.2, 2.1, 2.2, 3.1, 4.1, 5.2, 7.1, 8.2 (Core); Def 2.1, 4.1 (Execute); Def 3.1 (Q99); Def 4.1 (Conformance) -- all in obligations above.
- **Protocol/algorithm coverage:** explanation pipeline (O27), refactor protocol (O28), port obligation enumeration (O29), greenfield preservation set (O30), transactional lease+CAS+log-replay protocol (O31/O32), capability capture record (O33), continuation/CCNF handling (O23/O39), eviction-slack guard algorithm (O48), residency knapsack plan + independent check (O46).
- **Phase A overlap determined by grep against:** `00_CANONICAL_IMPLEMENTATION_PACK.md` (V6 map rows, canonical spec §6-8, fault program §8, claim gates §9, phases, ZS-* requirements), `ZERO_EXECUTE_SEMANTIC_ABI_V6.md`, `Q99_CAUSAL_CACHE_IMPLEMENTATION_SPEC_V6.md`, `V6_CANONICAL_SYSTEM_SPEC.md`, `V6_IMPLEMENTATION_MASTER_PLAN.md`, `IMPLEMENTATION_BACKLOG_V6_SUMMARY.md`, `V6_RESEARCH_AGENDA.md`. Items marked NO were confirmed absent via targeted grep of all of these.
- **Residual risk:** the pack is an extracted-text convenience copy; source PDFs (`current/model_ingest/text/pdf_text/current/papers/*.pdf` + LaTeX) remain authoritative for exact formula typography; line-level rendering of equations (e.g., subscripts in the knapsack proof) may differ from the source. Not cross-checked against pack 03 (History/Corrections/Auxiliary) or pack 02 (Draft 5 detail) -- out of scope for this scout.

**First file for the next agent:** `/tmp/racc_v6/ZeroStack_RACC_Cumulative_V6_Release/current/implementation/THEOREM_TO_RUNTIME_MAP_V6.md` -- to add the 4 unmapped process theorems (O27-O30) as new rows before any implementation claim touches explanation/refactor/port/greenfield paths.
