# fresh-work-vector/v1

Normative contract for `crates/zero-ledger/src/fresh_work.rs`.

The four components of an action's declared input sum exactly to
`total_tokens`. `eta_action` = fresh work / total as integer parts-per-million
in `[0, 1_000_000]`. Aggregation is checked integer addition.
