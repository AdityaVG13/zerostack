# World-fork latency gate (fszero-ap9)

Generated by `benchmarks/world_fork.py` — do not hand-edit numbers.
Hardware: Apple M5 Max / 18 cores. Commit: `8adcab58e5c3` (dirty=false). Date: 2026-07-10T06:47:42Z.
Method: per-fork = (wall of 200-fork plan - wall of 1-fork plan) / 199, medians over 3 runs; corpora are deterministic synthetic trees (`gen_corpus.py`, seed 42). Fork is copy-on-write: no tree scan, no file reads, no store writes.

| files | median per-fork ms | gate (<10ms) |
| --: | --: | :-- |
| 23000 | 0.009 | PASS |
| 100000 | 0.010 | PASS |

Size independence: 100000/23000 files = 4.3x repo, per-fork ratio 1.02x.

Reproduce: `python3 benchmarks/world_fork.py` (requires the release-perf binary; `./scripts/profile_build.sh -p fs-zero --bin fszero`). Raw runs in `world-fork.json`.
