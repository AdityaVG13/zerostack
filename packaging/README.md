# Packaging

This repository is source-only. There is no public ZeroStack package.

Do not `brew install`, `npm install`, or `pi install` anything from this tree.
There is no tagged release, bottle, registry tarball, or Pi extension to download.

Local development uses the Cargo workspace and `bindings/node` from source.
