{"id":"zerostack-0ne","title":"workspace clippy fails to compile and fmt is dirty in zero-abi and conformance","description":"cargo clippy --workspace --all-targets currently FAILS to compile, and cargo fmt --all --check reports diffs, both from code that predates the zero-store work. This blocks adding a lint gate to CI (zerostack-conformance-refresh-ci-30a).\n\n  error: this loop never actually loops   (x2)\n  error: could not compile zero-abi (lib) due to 2 previous errors\n  warning: enclosing Ok and ? operator are unneeded   -- zerostack-codemode-conformance (lib)\n  Diff in conformance/src/checks.rs:115\n\nVerified not caused by this session: git diff --stat HEAD -- crates/zero-abi conformance is empty.\n\nNote these are clippy ERRORS, not warnings, so they fail without -D warnings. zero-store itself is clippy-clean and fmt-clean.\n\nFix, then wire cargo clippy --workspace --all-targets -- -D warnings and cargo fmt --all --check into CI as part of 30a.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-07-25T08:35:24.382821Z","created_by":"aditya","updated_at":"2026-07-25T10:11:02.482493Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-2tk","title":"wave 3: zero-mcp — shared JSON-RPC 2.0 + MCP stdio server core","description":"TZ jsonrpc.rs (928) + stdio.rs (484), FZ mcp_protocol/ (stdio 276 + http 377), GZ cli/mcp.rs (1258) each hand-roll JSON-RPC framing, initialize handshake, tools/list plumbing, error envelopes, stdio loop. MCP spec compliance is drift-forbidden — protocol layer belongs in one crate; engines supply a tool registry + dispatch closure. Also carries the FZ HTTP transport so any engine can opt in. Per-engine tool schemas stay engine-owned (decided).","status":"open","priority":2,"issue_type":"task","owner":"adityavgcode@gmail.com","created_at":"2026-07-24T09:20:39Z","created_by":"AdityaVG13","updated_at":"2026-07-24T09:20:39Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"zerostack-2x7","title":"CAS size-policy divergence: TokenZero and FSZero publish objects the hub and GraphZero refuse to read","description":"The hub and GraphZero cap objects at 256 MiB and return PolicyDenied above it (ZeroStack/crates/zero-store/src/cas.rs:17 and :156; GraphZero/crates/graphzero-store/src/store/shared_cas.rs:26 and :99). TokenZero and FSZero have no size policy in publish or resolve at all.\n\nSo TokenZero or FSZero can publish a >256 MiB object into a shared store root that a hub-based or GraphZero-based reader can never read: same digest, same path, hard PolicyDenied. Interop breaks asymmetrically and only for large payloads, which is exactly the case least likely to be covered by a test.\n\nFix: one policy constant in zero-store, enforced identically on publish and on read. Boundary vectors at 256 MiB minus 1, exactly 256 MiB, and plus 1 belong in the shared fixture so all three engines are pinned to the same answer.\n\nNote the related asymmetry in hash validation: hub and TokenZero and GraphZero build object paths by slicing hash[..2] without validating the input is hex first (hub cas.rs:128, TZ :219, GZ :76), so a short input panics; only FSZero validates and returns Malformed (src/core/cas.rs:140-143). Fix that in the same pass.\n\nPart of zerostack-sce part A wave 1.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-07-25T08:23:42.338318Z","created_by":"aditya","updated_at":"2026-07-25T10:11:02.573501Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-34x","title":"pi-zerostack extension has no git repo, so extension fixes cannot be committed, reviewed, or shipped","description":"The origin/main backend mechanism exists so that pushing to origin/main rebuilds the live pi-zerostack binaries. The extension that implements it cannot itself be delivered that way, because ~/Developer/pi-stack is not a git repository:\\n\\n  cd ~/Developer/pi-stack && git rev-parse --show-toplevel\\n  fatal: not a git repository (or any of the parent directories): .git\\n\\nThere is no .git anywhere up the tree, and no remote.\\n\\nConsequences:\\n1. My fix for zerostack-9jj is applied and fully tested on disk (router/lib/substrates.js plus two new tests in router/test/substrates.test.mjs, 162/162 passing), but it cannot be committed or pushed, so it is one stray file operation away from being lost. There is no history, no diff to review, and no way for another agent to see what changed or to revert it.\\n2. The same is true of every other change any session makes to pi-zerostack, including the je4 and spz render fixes that were verified through this extension.\\n3. Reviewing extension changes is impossible. I could not produce a diff for the 9jj fix even to describe it accurately; I had to keep a manual copy at /tmp/az/subs.fixed.js to restore after a RED/GREEN experiment. A tmp copy is the only backup that currently exists for anything here.\\n\\nThe package declares \"private\": true and version 0.1.2, and Pi loads it by path from ~/.pi/agent/settings.json:\\n  \"../../Developer/pi-stack/packages/pi-zerostack\"\\nso it is a live, load-bearing component with no version control at all.\\n\\nWanted, in order:\\n1. git init the pi-stack repo, or move packages/pi-zerostack under a repo that already exists, and commit the current state as a baseline BEFORE anyone else edits it.\\n2. Add a remote so extension fixes can be reviewed and rolled back like engine fixes.\\n3. Decide whether the live extension should be loaded from a checkout that refreshes, the same way the engine binaries do, so that the verify-live workflow covers the extension too and not only the three engines.\\n\\nUntil then, treat any pi-zerostack edit as unbacked: take a copy first. Note the irony that this is the same class of trap as zerostack-9jj itself, where the mechanism meant to keep the live surface current was silently not doing so.\\n\\nBlocks delivery of the zerostack-9jj fix.","status":"open","priority":1,"issue_type":"bug","created_at":"2026-07-25T09:52:13.845612Z","created_by":"aditya","updated_at":"2026-07-25T09:52:13.845612Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-3tc","title":"br create has no --description-file or stdin input; long evidence must go through argv","description":"Found while filing beads on 2026-07-25. Minor but it will bite scripted/agent use.\n\nbr create ADVERTISES FLAGS ITS USAGE LINE REJECTS\n  br create \"title\" -t bug -p 1 --description-file /tmp/d.txt\nfails with exit 2 and\n  Usage: br create --type <TYPE> --priority <PRIORITY> --description <DESCRIPTION> <TITLE>\nSo --description-file is not accepted, and the usage line implies --description is REQUIRED and positional-title-last. The workaround is\n  br create \"title\" -t bug -p 1 -d \"$(cat /tmp/d.txt)\"\nwhich works, but shoving a multi-KB description through argv is exactly what a --description-file flag exists to avoid, and it is fragile: the body must survive shell quoting, and long descriptions risk ARG_MAX.\n\nTwo things worth fixing:\n1. Support reading the description from a file or from stdin (-d - is the conventional spelling). Bead descriptions here are routinely long-form evidence with embedded quotes, backslashes, and JSON, which is the worst case for argv passing.\n2. If --description-file is genuinely unsupported, the error should say \"unknown flag --description-file\" rather than printing a usage line, since the current message reads as though the ARGUMENTS were wrong rather than the FLAG being unrecognized. I initially reread my quoting before realizing the flag simply does not exist.\n\nLow priority, but agents writing detailed beads hit this immediately, and the failure mode teaches the wrong lesson about what went wrong.","status":"closed","priority":4,"issue_type":"bug","created_at":"2026-07-25T09:12:06.361859Z","created_by":"aditya","updated_at":"2026-07-25T10:04:51.218642Z","closed_at":"2026-07-25T10:04:51.218585Z","close_reason":"Upstream, not fixable in these repos: br is a prebuilt binary from github.com/Dicklesworthstone/beads_rust with no local source. Re-tested against 0.2.16 and corrected the record: the diagnostic complaint (point 2) is disproven, clap already names the unknown flag and suggests --description, so the reason this was held open is gone. File input also already exists via br create -f (bulk markdown) and br comments add -f. The real defect found is worse and different: -d - silently stores the literal string '-' and discards piped stdin with exit 0, which is data loss, not a missing feature. ARG_MAX confirmed real, 1MB works and 2MB fails at exec. Full detail and a suggested upstream report in the comment.","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0,"comments":[{"id":1000007,"issue_id":"zerostack-3tc","author":"QuietSummit","text":"Not actionable in these repos. br is an external prebuilt binary at ~/.local/bin/br (dated Jul 15) with no source checkout anywhere under ~/AI or ~/Developer. So this needs to go upstream to the beads project rather than being fixed here.\n\nDowngrading priority accordingly. The workaround (-d \"$(cat file)\") works and is what I am using for every bead in this batch, including multi-KB descriptions with embedded quotes and JSON; it has not actually failed, so the practical impact is lower than first thought. The real risk is only ARG_MAX on a very large description.\n\nKeeping the bead open as an upstream-tracking item rather than closing it, since the diagnostic complaint in point 2 of the description still stands: an unknown flag should be reported as an unknown flag, not as a usage line implying the arguments were malformed.\n","created_at":"2026-07-25T09:16:59Z"},{"id":1000011,"issue_id":"zerostack-3tc","author":"AzureOrchid","text":"Re-tested this directly against br 0.2.16 in a throwaway repo. QuietSummit's routing call is right, this is upstream (github.com/Dicklesworthstone/beads_rust, confirmed from strings on the binary; no source checkout exists locally). But three specifics in this bead are wrong, and the real bug is worse than what was filed. Correcting the record so any upstream report is accurate.\n\nWRONG, POINT 2 OF THE DESCRIPTION. br does NOT print a bare usage line for an unknown flag. Actual output:\n  error: unexpected argument '--description-file' found\n  tip: a similar argument exists: '--description'\n  Usage: br create --type <TYPE> --priority <PRIORITY> --description <DESCRIPTION> <TITLE>\nThat is clap doing the right thing: it names the unknown flag AND suggests --description. The usage line is trailing context, not the whole message. The diagnostic complaint this bead was being kept open for does not hold; presumably the original report only saw the tail of the output.\n\nWRONG, THE USAGE LINE IS NOT THE REAL SIGNATURE. br create --help gives \"Usage: br create [OPTIONS] [TITLE]\". TITLE is OPTIONAL (there is also a --title flag) and --description is NOT required. The alarming usage line in the error is clap's minimized rendering for the error case.\n\nWRONG, FILE INPUT ALREADY EXISTS, IN TWO PLACES.\n- br create -f/--file does bulk import from markdown, splitting on '## Title' headers. Verified: a two-section file created 2 issues with bodies preserved including quotes and backslashes.\n- br comments add -f/--file reads comment text from a file. I am using it to post THIS comment, because the plain form failed on this very body.\nSo the capability the bead asks for is present for comments and for bulk create; what is genuinely missing is only single-issue description-from-file.\n\nTHE ACTUAL BUG, WORSE THAN WHAT WAS FILED. -d - does not read stdin. It silently stores the literal string \"-\":\n  cat d.txt | br create \"stdin-probe\" -t task -p 4 -d -\n  => description == '-'\nNo error, exit 0. Piped content is discarded. That is SILENT DATA LOSS, not a missing feature. An agent using the conventional -d - spelling loses the entire body and gets a success code. This should lead the upstream report.\n\nARG_MAX IS REAL AND REPRODUCIBLE, upgrading it from theoretical. ARG_MAX here is 1048576. Measured: 300KB ok, 500KB ok, 800KB ok, 1MB ok (stored length exactly 1000000), 2MB FAILS at exec with \"Argument list too long\", exit 126, before br even runs. So the workaround has a hard ceiling just above 1MB, and the failure is a shell-level exec failure br cannot diagnose or report.\n\nWORKAROUND IS FIDELITY-CLEAN, one caveat. -d \"$(cat file)\" round-trips embedded quotes and backslashes exactly. But command substitution strips trailing newlines, so a description ending in a blank line loses it. Irrelevant for prose; relevant for a trailing fenced code block.\n\nSUGGESTED UPSTREAM REPORT, in priority order:\n1. -d - silently stores \"-\" instead of reading stdin. Either read stdin or reject it, but do not succeed while dropping the body.\n2. Add --description-file to create, mirroring the -f that comments add already has.\n3. No diagnostic change needed; the error message is already good.\n\nNothing to fix in these four repos.ZEOF\nwc -c /tmp/az/3tc.txt\n","created_at":"2026-07-25T10:04:40Z"}]}
{"id":"zerostack-9g4","title":"benchmark and tracker artifacts record the authoring machine path (cosmetic, prevent new ones)","description":"The hub is the public repository, and .beads/issues.jsonl is tracked in it. That file embeds this machines home directory in two ways:\n\n1. Every issue record carries \"source_repo_path\":\"~/AI/ZeroStack\", written by br itself for all issues, so this is systemic and not a one-off typo.\n2. Several descriptions and comments quote absolute paths such as ~/AI/TokenZero while explaining cross-repo findings.\n\nCount at time of filing: 11 matching lines in the HEAD commit alone. Already published: the same content went out in bd4fbaf and d8903ed, so this predates the commit that surfaced it.\n\nThis directly contradicts AGENTS.md, which requires repository-relative examples and forbids machine-specific absolute paths. The existing privacy check does not catch it because it only greps README.md, docs, and AGENTS.md:\n\n  rg -n /Users/|/home/|BEGIN .*PRIVATE KEY|api[_-]?key|password README.md docs AGENTS.md\n\nDecisions needed, both for the user:\n- Stop tracking .beads/issues.jsonl in the public hub, or keep tracking it and scrub. Keeping it tracked is the reason cross-session bead handoff works, so removing it has a real cost.\n- Whether to rewrite history for the already-pushed commits, or accept the existing exposure and only prevent new leaks.\n\nConcrete remediation regardless of that choice:\n- Extend the privacy check to cover .beads/issues.jsonl.\n- Ask whether br can be configured to omit or relativize source_repo_path; if not, add a pre-commit scrub that rewrites it to the repo name.\n- Sweep existing descriptions and comments to repository-relative paths.\n\nExposure is a username and directory layout, not credentials: the scan found no keys, tokens, or private material.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-07-25T08:36:51.413057Z","created_by":"aditya","updated_at":"2026-07-25T09:57:45.413328Z","closed_at":"2026-07-25T09:57:45.413287Z","close_reason":"Prevention landed in ZeroStack eb2b7b7. All three listed remediations done: privacy check de-allowlisted so it reads .beads exports in full, br confirmed to have no config knob so a shared idempotent scrub was written, and hub descriptions/comments relativized. Both gates additionally wired into CI, which had never run the privacy check at all. No history rewrite, file stays tracked, per the user decision. Per-repo coverage is zerostack-ih3.","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0,"comments":[{"id":1000004,"issue_id":"zerostack-9g4","author":"aditya","text":"Narrowed after checking what actually ships. Downgrading: nothing is hardcoded to one machine in consumable code, so this is cosmetic metadata leakage, not a functional defect.\n\nVerified clean:\n- TokenZero shipped source has no host-absolute paths. The only /Users/ hit in real code is crates/tokenzero-engine/src/binary_resolve.rs:10, a doc comment asserting the opposite: \"No host-absolute personal paths (e.g. /Users/<you>/AI/.../target/release) are consulted.\" The other source hits are synthetic path-traversal fixtures in crates/tokenzero-install/src/package_audit/tests/tar.rs:829,866,902 using C:~ and ~, fake by design.\n- No tracked Cargo.toml in ZeroStack, TokenZero, or FSZero has a path dep escaping its own repo. FSZeros was the only real one and it is now the pinned hub git dep.\n\nActual scope is wider than .beads but shallower than first filed. Tracked files containing /Users/ or /home/, excluding .beads: TokenZero 26, GraphZero 6, FSZero 1, hub 1 (AGENTS.md, where it is the privacy-check pattern itself). Nearly all are recorded run metadata where a harness stamped its cwd into evidence JSON: benchmarks/northstar/*, benchmarks/boot-cost/*, benchmarks/claims/*, crates/tokenzero-mcp/benches/*, docs/contracts/zeroref-conformance-evidence.json, GraphZero bench/*, and scripts/semantic_golden_set.sh.\n\nTokenZero is public as well, so the benchmark artifacts are a larger surface than the hubs issues.jsonl. Exposure is a username and directory layout, no credentials, keys, or private material.\n\nUser assessment: acceptable, it is just a file path. So the remaining work is prevention, not remediation:\n- Make benchmark and conformance harnesses record repo-relative paths, or omit cwd, so new artifacts stop carrying it.\n- Extend the AGENTS.md privacy check beyond README.md, docs, and AGENTS.md to cover benchmarks, docs/contracts, and .beads.\n- No history rewrite. Not worth it for a home directory path.\n\nSeparately confirmed while checking: GraphZero/crates/graphzero-test-support/Cargo.toml:23 still path-deps ../../../TokenZero/crates/tokenzero-recovery, so a standalone graphzero clone cannot build. That is a real defect but it is graphzero-ml4r, not this bead.","created_at":"2026-07-25T08:41:27Z"}]}
{"id":"zerostack-9jj","title":"origin/main backend checkouts are 42h stale despite a 5min refresh interval; pushes never go live","description":"The pi-zerostack origin/main backend checkouts are not refreshing on their configured interval, so pushing to origin/main does NOT get the live binaries rebuilt. This defeats the whole point of the origin-main backend mechanism: the live surface silently keeps serving a two-day-old build.\n\nbackend_status reports:\n  origin_main_backends.enabled   true\n  origin_main_backends.refresh_ms 300000   (5 minutes)\n  build_timeout_ms                3600000\n  auto_init \"on first zero_execute: origin/main backend checkout refresh/build\n             is scheduled in the background\"\n\nObserved on 2026-07-25 at 05:22 local, after many zero_execute calls this session:\n\n  ~/.pi/agent/zerostack-origin-main/tokenzero.status.json\n    rev          27d99c39ac275e80e9513fa4f0f11a19564e2778\n    refreshedAt  2026-07-23T11:47:01.479Z\n    built        false\n    buildEnabled true\n    buildRequired false\n\n  ~/.pi/agent/zerostack-origin-main/graphzero.status.json\n    rev          887b24dcdb722e481e3807803cbf8840c751ee12\n    refreshedAt  2026-07-23T11:55:30.714Z\n    built        true\n\nrefreshedAt is ~42 HOURS stale against a 5-minute interval. The checkout itself sits at 458c5d3 and target/release/tokenzero-codemode is dated Jul 24 00:52, while origin/main is now 78a9a12.\n\nNo refresh or build process was running: ps for cargo/git under zerostack-origin-main showed nothing but my own grep.\n\nNote tokenzero.status.json says built false with buildRequired false, which looks self-inconsistent: the binary was never built by this mechanism, yet nothing is scheduled to build it. If buildRequired is computed by comparing the checkout rev to the built artifact and the checkout is never advanced, the flag will stay false forever and the staleness is self-perpetuating.\n\nAlso note the directory casing is inconsistent, which may or may not matter on a case-insensitive filesystem: the tz checkout is at .../zerostack-origin-main/TokenZero while a sibling lowercase .../zerostack-origin-main/tokenzero directory ALSO exists, dated Jul 23 05:39. Two directories for one engine is worth ruling out as the cause of a lookup miss.\n\nImpact: an agent that pushes a fix to origin/main and then tests through zero_execute is testing the OLD binary while believing it is testing the new one. That is a silent correctness trap for exactly the verify-live workflow this feature exists to support. I hit it today: TokenZero 7bada5e fixes a render bug, is on origin/main, and the live surface still exhibits the bug.\n\nWanted: refresh actually firing on the interval (or on demand), an explicit way to force a refresh/rebuild, and a status surface that makes staleness obvious rather than requiring a manual read of the status JSON.","status":"closed","priority":1,"issue_type":"bug","assignee":"AzureOrchid","created_at":"2026-07-25T09:23:14.585538Z","created_by":"aditya","updated_at":"2026-07-25T09:52:50.410452Z","closed_at":"2026-07-25T09:52:50.410414Z","close_reason":"Root-caused and fixed: config-override branch returned before originMainBinaryFast, the only caller of scheduleOriginMainRefresh, so the refresh could never fire. Fix routes managed paths through the scheduler. Regression test proven RED before GREEN; 162/162 extension tests pass. Delivery blocked on zerostack-34x (pi-stack has no git repo). See comment for full evidence.","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-ajy","title":"conditional wave 5: 1TP atoms/gauge/ordinal grammar as zero-gauge","description":"GZ plans 1TP adoption (graphzero-0odn: symbol ordinals, gauge, TA registry) and FZ may follow. TZ owns one-token-atoms fixtures, gauge arithmetic, tz://o/<gen>/<ord> ordinal grammar today. If 2+ engines speak 1TP it is a shared protocol: fixtures, atom certification checker, ordinal grammar + allocation arithmetic move to zero-gauge — also the right place to fix the omega-math finding that standalone digit atoms do not certify composed refs (certify complete grammar instances in context, per provider). Trigger: when graphzero-0odn is scheduled. Do NOT extract before then.","status":"open","priority":3,"issue_type":"task","owner":"adityavgcode@gmail.com","created_at":"2026-07-24T09:20:41Z","created_by":"AdityaVG13","updated_at":"2026-07-24T09:20:41Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"zerostack-b3l","title":"wave 4: capability negotiation + cross-engine telemetry schema into shared crates","description":"(A) Capability peer validation: FZ core/capability.rs DescriptorFields tolerant extractor, GZ zeroref_capability.rs validate_peer_descriptor, TZ mcp/capability_descriptor.rs — each engine parses the OTHER engines descriptors with alias tolerance (hash.algo vs hash.algorithm, layout_version key drift). This code exists only to reconcile drift; sharing the descriptor schema + validator removes the reason it exists. Engines keep engine-owned fields (remediation, limits). Per-dimension clamp policy vocabulary ({byte,line_start,line_end}, cf. FZ b369c99) becomes the shared type. (B) zero-telemetry: prevented_* counter schema GZ writes into the TZ session ledger (graphzero-ay7m) is cross-engine wire format — types + canonical serialization shared, sinks engine-owned.","status":"open","priority":2,"issue_type":"task","owner":"adityavgcode@gmail.com","created_at":"2026-07-24T09:20:40Z","created_by":"AdityaVG13","updated_at":"2026-07-24T09:20:40Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"zerostack-clamp-policy-contract-owner-7ca","title":"declare canonical zero-ref line-clamp policy per dimension (two engine p1s wait on this)","description":"crates/zero-ref already implements the mechanism: LineEndPolicy::Strict and LineEndPolicy::ClampEnd, with select_fragment defaulting to Strict (crates/zero-ref/src/lib.rs, select_fragment at ~line 345, policy match at ~line 410).\n\nWhat is missing is the contract decision about which policy is canonical for which dimension, and whether the capability descriptor must advertise it. Without that, engines diverge by default:\n- TokenZero clamps (tokenzero-52gv, p1)\n- shared strict selection errors with RangeOutOfBounds\n- FSZero calls select_fragment_with_policy explicitly at src/core/zeroref.rs:40 (fszero-00vq, p1)\n\nSo the same ref over a short file can succeed in one engine and error in another, which is exactly the drift the shared crate exists to prevent.\n\nToday the only hub-side mention is inside zerostack-b3l, a p2 wave-4 bead covering the per-dimension clamp policy vocabulary ({byte, line_start, line_end}, cf. FSZero b369c99). That is a priority inversion: two p1 engine beads are blocked on a p2 hub decision. This bead pulls the decision out of b3l so it can land independently of the capability-negotiation work.\n\nDeliverables:\n1. Decide and document canonical policy per dimension in the hub contract.\n2. Encode it in zero-ref as the default, so engines cannot silently pick differently.\n3. Extend the golden-vector fixture with clamp and strict cases at the boundary.\n4. State whether the capability descriptor must advertise the policy, and if so add it to the conformance schema.\n\nUnblocks: tokenzero-52gv, fszero-00vq.","notes":"Implemented canonical v1 bounds policy: byte strict, line_start strict, line_end clamp. zero-ref defaults now encode ClampEnd; explicit strict APIs remain for validation. Fixture v2 includes canonical clamp and strict override boundary vectors. docs/zeroref.md states policy is fixed by ZeroRef v1 and not separately capability-negotiated. Verified on spark-1672 via rch: zero-ref 4/4 tests passed; clippy -D warnings passed; rustfmt and git diff --check passed. Independent review's versioning wording concern was fixed by clarifying this establishes the previously unspecified v1 default.","status":"closed","priority":1,"issue_type":"task","assignee":"QuietSummit","created_at":"2026-07-25T07:09:06.290324Z","created_by":"aditya","updated_at":"2026-07-25T08:21:57.211106Z","closed_at":"2026-07-25T08:21:57.211064Z","close_reason":"Canonical ZeroRef bounds policy implemented and documented. Golden fixture v2 covers clamp and strict boundaries. Remote Spark tests and clippy pass.","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-conformance-refresh-ci-30a","title":"conformance suite is stale and unwired: all reports predate three weeks of engine work","description":"The hub owns conformance/ but nothing keeps it current.\n\nEvery artifact in conformance/reports is dated 2026-07-01: fz-*, gz-*, tz-* (23 files). Three weeks of engine changes have landed since, including zero-abi and zero-ref adoption in all three engines. The suite is not run by any CI workflow in this repo.\n\nMeanwhile the engine side reports a contradiction worth resolving here: GraphZero closed zeroref-v1-shared-cas-1ghi.8 and 1ghi.9 as done, while their own notes say external blockers were unresolved and the conformance gate deliberately stays BLOCKED with docs/contracts/zeroref-conformance-evidence.json in status pending. So the hub currently has no independent evidence that the engines conform, and the engine tracker records a closed-but-unproven state.\n\nDeliverables:\n1. Re-run the conformance suite against current engine HEADs, one report per engine, committed.\n2. Wire it into hub CI so a stale suite fails rather than silently rots.\n3. Record the report freshness contract: which engine revs each report attests to.\n4. Reconcile the GraphZero pending-evidence contradiction, either by producing passing evidence or by reopening the closed beads.\n\nNote: hub workspace is edition 2021 while all three engines are edition 2024 on nightly, so the suite must keep compiling under both.","status":"open","priority":1,"issue_type":"task","created_at":"2026-07-25T07:09:06.419758Z","created_by":"aditya","updated_at":"2026-07-25T07:09:17.443455Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"zerostack-conformance-refresh-ci-30a","depends_on_id":"zerostack-clamp-policy-contract-owner-7ca","type":"related","created_at":"2026-07-25T07:09:17.443355Z","created_by":"aditya","metadata":"{}","thread_id":""}]}
{"id":"zerostack-engine-zero-store-cutover-ee2","title":"zero-store adoption: no engine consumes it yet, three local CAS copies still live","description":"Gap found while surveying all three engines. The hub shipped crates/zero-store, but every engine pins only zero-abi and zero-ref at rev 90cdbfad5fa5bc5d601e841def2416d64810a487. Nothing depends on zero-store, so the extraction has produced no deduplication yet.\n\nVerified local CAS/store code still owned per engine:\n- TokenZero: crates/tokenzero-recovery/src/shared_cas.rs (1485 LOC, includes the zerostack.cas-gc.v1 GC), embedded_store.rs, segment_store.rs, crates/tokenzero-mcp/src/codemode/store.rs, crates/tokenzero/src/zerostack_store.rs\n- FSZero: src/core/cas.rs (341 LOC), src/core/zerostack_store.rs, src/core/embedded_store.rs, src/core/cdc.rs, src/core/ref_resolver.rs, src/core/recovery/*\n- GraphZero: crates/graphzero-store/src/store/blob_store.rs, store/ref_index.rs, store/fs_replace.rs, blob_put path in store/indexer.rs:2310-2407\n\nFSZero has no bead of its own for this at all. TokenZero tracks it only indirectly via tokenzero-9d2x, which names \"Wave 1.5 zero-store cutover\" as its fix vehicle. GraphZero tracks fragments via store-schema-version-93rn and zeroref-v1-shared-cas-1ghi.\n\nThis bead owns the engine-side adoption sequencing that zerostack-sce (the code move) does not cover. Blocked on zerostack-sce landing GC plus store-root resolution, because adopting today would mean adopting a strictly weaker store.\n\nEquality gate per engine: digests byte-identical before and after, and store-path golden tests proving the same inputs resolve to the same paths.","status":"open","priority":1,"issue_type":"task","created_at":"2026-07-25T07:09:06.227291Z","created_by":"aditya","updated_at":"2026-07-25T07:09:17.374927Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"zerostack-engine-zero-store-cutover-ee2","depends_on_id":"zerostack-sce","type":"blocks","created_at":"2026-07-25T07:09:17.374823Z","created_by":"aditya","metadata":"{}","thread_id":""}]}
{"id":"zerostack-g25","title":"GraphZero declares mutation \"store-only\", a value the contract does not define","description":"Found by the first live conformance run after the harness was repaired (ZeroStack 2241b75). G7 limits and G8 mutation both FAIL against GraphZero for the same reason.\n\nCONTRACT.md section 2 defines exactly three mutation values and pins gz:\n  | GraphZero | gz | readonly |\n  readonly: same rejection behavior as denied, and the substrate additionally\n  declares that no mutating substrate operations exist in CodeMode. Read-only\n  metadata/ref writes needed to store execution records are not domain mutation.\n\nGraphZero declares a fourth value instead:\n  crates/graphzero-query/src/codemode/discovery.rs:33   \"mutation\": \"store-only\"\n  crates/graphzero-query/src/codemode/discovery.rs:129  \"mutation_surface\": \"store-only\"\n  crates/graphzero-query/tests/codemode_e2e.rs:661      asserts \"store-only\"\nwith the inline rationale \"remember/reserve/index mutate GraphZero's own store\".\n\nBoth sides have a real argument:\n- The contract's readonly clause already carves out \"read-only metadata/ref writes needed to store execution records\", but remember/reserve/index are more than execution bookkeeping; they are caller-initiated writes to GraphZero's store. Calling that readonly is arguably a lie.\n- Equally, the contract is normative and enumerates three values. An engine unilaterally inventing a fourth is exactly the drift the conformance suite exists to catch, and any consumer switching on mutation will hit an unknown variant.\n\nNote the vocabulary ALSO drifts inside GraphZero: the CodeMode surface says \"store-only\" (hyphen) while the operation ABI says \"store_only\" (underscore) at operation_abi/digest.rs:64 and operation_abi/vectors.rs:76. Whatever is decided, one spelling.\n\nThis needs a contract owner, not a guess. Two coherent resolutions:\nA. Add store-only to CONTRACT.md section 2 as a fourth value with defined semantics (which operations may write, what a consumer must assume), repin gz to it, and update the conformance schema + harness validator. Pick one spelling.\nB. Declare readonly is correct because GraphZero's writes are store-internal and not domain mutation, and change GraphZero to declare readonly.\n\nBlocks a passing gz attestation, so it blocks zerostack-conformance-refresh-ci-30a deliverable 4 (reconcile the GraphZero closed-but-unproven state). Related: zerostack-hix.","status":"open","priority":1,"issue_type":"bug","created_at":"2026-07-25T09:07:38.959494Z","created_by":"aditya","updated_at":"2026-07-25T09:07:38.959494Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-graphzero-canonical-json-drift-881","title":"GraphZero has a second canonical-JSON implementation off zero-abi","description":"Undetected drift, no bead in any repo before this one.\n\nGraphZero/crates/graphzero-pack/src/manifest.rs:85 defines its own unsigned_canonical_json. GraphZero already re-exports zero_abi canonical JSON elsewhere (crates/graphzero-query/src/operation_abi/schema.rs re-exports zero_abi canonical_json, canonical_schema_json, normalize_schema, schema_diff, and digest.rs:53,58 call zero_abi contract_digest*), so this one function is a parallel implementation of the single most drift-forbidden primitive in the stack.\n\nCanonical JSON feeds the contract digest. If the pack manifest hashes bytes that zero-abi would encode differently, pack manifests and contract digests disagree about identity, and the disagreement is silent because both sides produce valid-looking hashes.\n\nDeliverables:\n1. Confirm whether unsigned_canonical_json is byte-identical to zero_abi canonical_json today. Differential test over the zero-abi fixture corpus plus pack manifest fixtures.\n2. If identical, delete it and call zero-abi.\n3. If not identical, that is a live digest bug: record which manifests were hashed with the divergent encoder before replacing it.\n4. Add a hub-side guard so a second canonical-JSON implementation cannot reappear unnoticed.\n\nRelated: tokenzero-hks0 (canonical-JSON digest hardening against zero-abi), tokenzero-ghv7 (contract digest depends on registry declaration order).","status":"open","priority":2,"issue_type":"bug","created_at":"2026-07-25T07:09:06.355298Z","created_by":"aditya","updated_at":"2026-07-25T10:11:02.755089Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0,"comments":[{"id":1000005,"issue_id":"zerostack-graphzero-canonical-json-drift-881","author":"QuietSummit","text":"Deliverable 1 DONE, and the answer is NO: the two encoders are not byte-identical.\n\nEvidence: GraphZero 41170ab adds crates/graphzero-pack/tests/canonical_json_drift.rs, 4 tests, all passing on spark via rch.\n\nRoot cause is narrower than the bead assumed. unsigned_canonical_json (graphzero-pack/src/manifest.rs:85) is plain serde_json::to_string over #[derive(Serialize)] on UnsignedPackManifestRef, so it emits DECLARATION order. zero_abi::canonical_json (ZeroStack crates/zero-abi/src/schema.rs:33) emits SORTED keys recursively. Both produce valid JSON that parses to an equal Value; only a hashing consumer can see the difference. So this is deliverable 3 (a live digest divergence), NOT deliverable 2 (identical, safe to delete).\n\nBlast radius, why I did NOT migrate in that commit: unsigned_canonical_json feeds digest_sha256 and is the exact payload ed25519-signed at sign.rs:9 and verified at sign.rs:16. Switching encoders changes every pack manifest digest AND invalidates every existing signature. That needs a recorded migration with a schema_version bump, not a drive-by.\n\nAlso note graphzero-pack did not depend on zero-abi at all (only graphzero-query did), which is how this went undetected. Added zero-abi as a dev-dependency so the gate exists without coupling the shipped crate.\n\nRemaining: deliverable 3 (record which manifests were hashed with the divergent encoder) and deliverable 4 (hub-side guard against a third canonical-JSON impl). Filing those separately as migration work.\n","created_at":"2026-07-25T09:00:10Z"}]}
{"id":"zerostack-hix","title":"conformance harness must test ONE installed surface, not a dual-surface binary","description":"The conformance harness cannot run against ANY current engine. This is worse than the staleness recorded in zerostack-conformance-refresh-ci-30a: the suite is not merely out of date, its core process model contradicts what all three engines now enforce.\n\nHARNESS ASSUMPTION (conformance/src/lib.rs):\n  :885  McpClient::spawn(bin, mode, timeout) runs Command::new(bin).arg(\"--mode={mode}\")\n  :477  check_exposure spawns ONE binary as --mode=codemode\n  :487  ...then spawns the SAME binary as --mode=mcp\n  G1 \"exposure\" is defined as: the codemode surface exposes exactly the codemode tools, AND the mcp surface exposes none of them. That check is only meaningful if one artifact can serve both.\n\nENGINE REALITY (verified by direct invocation, 2026-07-25, all three release binaries):\n\n  TokenZero target/release/tokenzero-mcp --mode=codemode\n    exit 101: \"artifact is locked to surface 'mcp'; refused --mode=codemode.\n    Install tokenzero-codemode for the CodeMode catalog (mutually exclusive).\"\n\n  FSZero target/release/fszero --mode=codemode\n    exit 2: \"packaging shim has no surface runtime compiled in. Install fszero-mcp\n    or fszero-codemode (selection is immutable), or invoke that artifact directly.\n    Do not pass --mode= to a dual-capable process - dual surfaces are forbidden.\"\n\n  GraphZero target/release/graphzero --mode=codemode\n    exit 1: \"the compatibility shim does not host MCP/CodeMode servers. Install\n    graphzero-codemode (selected surface) or rebuild the matching single-surface artifact.\"\n\nTokenZero enforces this at COMPILE time, not just runtime: building --features \"surface-mcp,surface-codemode\" hits\n  compile_error!(\"tokenzero surfaces are mutually exclusive (tokenzero-irx9.3): enable exactly one of feature surface-mcp or surface-codemode - never both.\")\nSo a dual-surface tokenzero artifact cannot be produced at all. The harness's G1 is unsatisfiable by construction.\n\nThis explains why the reports froze on 2026-07-01: that is approximately when single-surface artifacts landed, and nobody could run the suite afterward. The 23 committed reports attest to a binary shape that no longer exists.\n\nREQUIRED CHANGE:\nRunConfig must take a surface pair, not one path. Suggested:\n  --codemode-bin <path> --mcp-bin <path>\nwith --bin kept as a back-compat alias meaning \"same artifact for both\", which is now only true for test fixtures like the built-in fake substrate.\n\nG1 exposure then becomes a cross-ARTIFACT check: the codemode artifact exposes exactly the codemode tool set, and the mcp artifact exposes none of it. That is the same invariant the check was always trying to express, and it stays meaningful under single-surface packaging. It arguably gets STRONGER, since surface separation is now enforced by the build rather than by a runtime flag.\n\nAlso fix while in here (found during the same investigation):\n- Reports record substrate_binary as an absolute authoring-machine path, e.g. \"/Users/<name>/AI/tokenzero/target/release/tokenzero\". That is a local-layout leak in a public repo. Record basenames. Overlaps zerostack-9g4.\n- ReportFile has no engine revision field, so no report can be tied to the code it attests to. Contract and checker for this already landed: conformance/reports/ATTESTATION.md and conformance/scripts/check_freshness.py.\n\nBLOCKS: zerostack-conformance-refresh-ci-30a deliverable 1 (re-run the suite against current engine HEADs). 30a deliverables 2 and 3 (CI wiring, freshness contract) are already done and do not depend on this.","status":"closed","priority":1,"issue_type":"bug","assignee":"QuietSummit","created_at":"2026-07-25T09:03:42.642129Z","created_by":"aditya","updated_at":"2026-07-25T10:10:55.594254Z","closed_at":"2026-07-25T10:10:55.594206Z","close_reason":"Fixed and pushed as a25c523. Harness now models one artifact + one Surface (bin/surface), infers surface from filename, refuses to guess on the bare shim, and G1 checks that the artifact serves its own surface AND refuses the opposite. G2-G10 only run against a codemode artifact. Verified against installed graphzero-codemode: G1 now PASSES (previously failed only because the harness demanded a second artifact that must not exist); G4/G5/G10 pass; remaining failures are the real contract divergences tracked as mpi and g25. Workspace fmt+clippy -D warnings+tests green.","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0,"comments":[{"id":1000006,"issue_id":"zerostack-hix","author":"QuietSummit","text":"Harness now runs against real engines (ZeroStack 2241b75). First live gz run produced ACTIONABLE findings, and they point at the HARNESS being wrong, not the engine.\n\nG2/G6 \"invalid CodeMode ref\": GraphZero emits\n  gz://codemode/execution/cm___exec_1784970306329-20532-0-2afa6c5e5632\nThe harness ref regex (conformance/src/lib.rs:64) is\n  ^{ns}://(blob/[0-9a-f]{64}|codemode/execution/[^/]+/(code|steps|telemetry|result|error))$\nwhich REQUIRES a trailing /code|/steps|/telemetry|/result|/error segment. The bare execution root ref has no suffix and cannot match. GraphZero also emits all four suffixed refs alongside it, so the engine looks correct and the regex looks too strict. Needs a contract decision: is the bare execution root a legal ref? If yes, make the suffix optional.\n\nG5 errors, G10 sandbox: harness reports \"case did not return structured error\", but the payloads clearly ARE structured, with correct kinds:\n  validation -> kind \"validation\"\n  sandbox    -> kind \"sandbox\"   (\"network/fetch denied\")\n  runtime    -> kind \"runtime\"\n  substrate  -> kind \"substrate\"\n  policy     -> kind \"policy\"    (\"CodeMode is read-only; mutating JS binding 'edit' is unavailable\")\nEvery one carries isError true, structuredContent.error.{kind,message,retryable}, and error_ref. So extract_json_payload is failing to find them. GraphZero wraps them in a content[] entry of type \"structuredContent\" AND repeats them at top-level structuredContent; the harness likely only looks at content[0].text and tries to parse it as JSON, but content[0].text here is the human-readable ack line, not JSON. Harness bug.\n\nG7 limits: \"invalid mutation \\\"store-only\\\"\". CONTRACT.md section 2 admits only allowed|denied|readonly, and pins gz to readonly. GraphZero declares store-only. This one may be a real engine or contract divergence: either GraphZero should declare readonly, or the contract needs a fourth value defined. Do not guess, needs a decision.\n\nG1 exposure: \"server exited while waiting for initialize response\" for the mcp side. graphzero (the shim) is not the mcp-surface artifact; there is probably a graphzero-mcp that was not built. Need the right artifact before G1 means anything.\n\nRecommend splitting: harness ref-regex + payload-extraction fixes are one bead, the mutation vocabulary question is another, since the second needs a contract owner.\n","created_at":"2026-07-25T09:06:06Z"},{"id":1000010,"issue_id":"zerostack-hix","author":"QuietSummit","text":"CORRECTION. My first fix was also wrong, in the same direction as the original bug.\n\nI replaced \"one binary, two --mode= spawns\" with \"--codemode-bin plus --mcp-bin\", i.e. two artifacts present at once. That still assumes both surfaces exist side by side. They do not. The product rule is EITHER/OR at INSTALL time:\n\n  crates/tokenzero-install/src/packaging.rs:3\n    \"Product rule: users install **one** of tokenzero-mcp or tokenzero-codemode\n     from the same revision and shared core. The installer writes one client\n     registration and replaces any prior surface. Dual catalog / dual mode\n     startup fails closed.\"\n  packaging.rs:13\n    \"The tokenzero name is a compatibility shim / selected symlink only -\n     never a dual-surface binary.\"\n\nSo you install ONE surface. Requiring both artifacts describes a machine state that is not allowed to exist, and on a normally-installed host the second path simply is not there. My version would have failed for the wrong reason.\n\nCorrect model, now implemented:\n  RunConfig { bin, surface }        one artifact, one surface\n  --bin <path> --surface codemode|mcp\n  surface inferred from the filename (<name>-codemode / <name>-mcp) so the\n  common case needs no flag. The bare shim is NOT guessed: it is a selected\n  symlink, so an unrecognizable name is a hard error rather than a default,\n  because silently running the wrong surface's checks yields misleading\n  evidence.\n\nG1 exposure is now a property of the ONE artifact under test, checked two ways:\n  1. it serves the tool set its surface implies (codemode artifact exposes\n     exactly the codemode tools; mcp artifact exposes none of them), and\n  2. it REFUSES the opposite surface. A single-surface artifact that also\n     answers as its opposite is precisely the dual catalog the product rule\n     forbids, so serving it is the failure and refusing it is the pass.\nThat second half is the invariant that actually protects the separation, and neither the original harness nor my first fix tested it.\n\nG2-G10 exercise CodeMode execution semantics, so they now only run against a codemode artifact. Against an mcp artifact there is no execute_code to drive; reporting those as failures was a category error.\n\nVERIFIED against the installed gz codemode artifact:\n  bin: graphzero-codemode\n  G1 exposure PASS      <- was FAIL \"server exited while waiting for initialize\"\n  G4 leak-proof PASS\n  G5 errors PASS\n  G10 sandbox-denial PASS\n  G2/G3/G6/G9 FAIL -> zerostack-mpi (no execution_id, no inline telemetry)\n  G7/G8 FAIL       -> zerostack-g25 (mutation \"store-only\")\nG1 was only ever failing because the harness insisted on a second artifact that must not exist. The remaining failures are the real contract divergences, unchanged.\n\nAlso verified the shim refuses to be guessed:\n  --bin .../graphzero\n  -> Error: cannot tell which surface \"graphzero\" serves; pass --surface codemode or --surface mcp\n","created_at":"2026-07-25T09:36:47Z"}]}
{"id":"zerostack-hxl","title":"distribution: prebuilt binaries, homebrew tap, unified installer, .zerostack layout doc","description":"Ship story for 1..3 engines + shared substrate. Facts: engines are statically linked single binaries; zerostack crates are compile-time git deps baked in at build — users never install zerostack; only builds fetch it (cargo via SHA pin). Deliverables: (1) GitHub Releases per engine (macos-arm64/x86_64, linux-x86_64/arm64, windows-x86_64) via CI matrix. (2) homebrew tap AdityaVG13/homebrew-zerostack (already referenced in GZ README): formulas tokenzero/fszero/graphzero + meta formula zerostack installing all three. (3) installer script hosted in THIS repo: curl | sh with --all or per-engine flags (winget/scoop manifests later). (4) docs/store-layout.md: canonical .zerostack/ on-disk contract — per-repo store, engine namespaces (fszero/repos/<sid>, graphzero/projects/<key>, tokenzero), shared CAS blobs unified by pinned layout version; installing engines in any order adds namespaces beside existing ones, no migration; version skew across engines guarded by capability negotiation (worst case read_only/disabled, never corruption). (5) later: publish zero-* to crates.io so cargo install tokenzero works without git — blocked on stable semver; keep SHA pins until then.","status":"open","priority":1,"issue_type":"task","owner":"adityavgcode@gmail.com","created_at":"2026-07-24T09:20:41Z","created_by":"AdityaVG13","updated_at":"2026-07-24T09:20:41Z","source_repo":".","compaction_level":0,"original_size":0,"comments":[{"id":1000000,"issue_id":"zerostack-hxl","author":"AdityaVG13","text":"DECISION (user): start with three separate binaries (tokenzero, fszero, graphzero) as three commands — matches the use-one-alone model and is simplest. Busybox-style single zero multiplexer binary deferred; revisit only if there is real demand.","created_at":"2026-07-24T09:25:43Z"}]}
{"id":"zerostack-ih3","title":"apply the beads-export scrub and privacy gates to TokenZero, FSZero, and GraphZero","description":"Follow-up to zerostack-sg3, which built and proved the shared scrub but deliberately applied it only to the hub.\\n\\nscripts/scrub_beads_export.py in the hub (ZeroStack eb2b7b7) works cross-repo. Run from the hub against each engine's export it reports:\\n  TokenZero  248 record(s) carry absolute host paths\\n  FSZero     310 record(s)\\n  GraphZero  846 record(s)\\n\\nAll three .beads/issues.jsonl files are tracked, and TokenZero is public, so this is the same exposure as the hub had: username and directory layout on every issue record via br's source_repo_path stamp, plus absolute paths quoted in descriptions and comments.\\n\\nWHY THIS WAS NOT DONE IN THE SAME PASS\\nScrubbing rewrites 248/310/846 records, which is a very large diff in each repo. At the time of filing, other sessions had substantial uncommitted work in those trees (I lost my own uncommitted TokenZero edits to a concurrent operation earlier in this session, see the note on zerostack-wth). Landing a whole-file rewrite of a tracked, actively-synced file underneath another agent is a good way to cause a conflict or destroy work. It should be done when those trees are quiet.\\n\\nDELIVERABLES\\n1. Run the scrub in TokenZero, FSZero, and GraphZero, one commit per repo, when the tree is quiet and the other sessions have committed.\\n2. Verify integrity the same way the hub was verified, and do not skip this: record count unchanged, id order unchanged, per-record key sets unchanged, and only source_repo_path, description, and comments differing. The hub check caught nothing wrong, but a silent field loss across 846 records would be very hard to notice later.\\n3. Copy or reference the two gates into each engine repo's CI, since a scrub with no gate will simply rot the moment the next br sync runs. The hub runs both check_no_host_paths.py and scrub_beads_export.py --check in a privacy job.\\n4. Add the scrub to each repo's documented br sync recipe, as was done in the hub AGENTS.md, so it is a required step rather than folklore.\\n\\nNote the ordering constraint: the scrub must run AFTER br sync --flush-only and BEFORE git add, because br rewrites the export from SQLite on every sync and will reintroduce the absolute path each time. A one-time scrub without the recipe change and the CI gate accomplishes nothing durable.\\n\\nDecide whether to vendor a copy of the script per repo or reference the hub copy. A copy per repo is more robust, since the engine repos should not require a hub checkout to pass their own CI, but then the four copies must be kept in sync.\\n\\nRefs zerostack-sg3, zerostack-9g4.","status":"open","priority":2,"issue_type":"task","created_at":"2026-07-25T09:57:23.929875Z","created_by":"aditya","updated_at":"2026-07-25T09:57:23.929875Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-j0j","title":"artifact cleanup can delete the active CodeMode backend and take zero.token offline","description":"Observed live 2026-07-25 while following the requirement to periodically clear stale build artifacts. Removing ~/AI/TokenZero/target also removed target/release/tokenzero, which the active pi-zerostack CodeMode session was using as its zero.token backend. backend_status immediately reported tokenzero:null; subsequent zero.token ref expansion failed with env path missing. The separately installed ~/.tokenzero/bin/tokenzero remained healthy, so this is backend lifecycle coupling, not source corruption. Deliverables: (1) active backend binaries must live in a protected origin-main runtime directory, never a developer target dir eligible for cleanup; (2) cleanup tooling must detect and refuse active backend paths; (3) backend_status should report the configured path and remediation; (4) regression smoke removes a repo target tree and proves zero.token remains live; (5) document safe stale-artifact cleanup. Recovery today is push origin/main and await backend rebuild.","status":"in_progress","priority":0,"issue_type":"task","assignee":"QuietSummit","created_at":"2026-07-25T10:07:45.257732Z","created_by":"aditya","updated_at":"2026-07-25T10:12:39.189128Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-j0z","title":"probe","status":"closed","priority":4,"issue_type":"task","owner":"adityavgcode@gmail.com","created_at":"2026-07-24T09:20:10Z","created_by":"AdityaVG13","updated_at":"2026-07-24T09:20:49Z","closed_at":"2026-07-24T09:20:49Z","close_reason":"db init probe, not real work","source_repo":".","compaction_level":0,"original_size":0}
{"id":"zerostack-je4","title":"shell repo-inventory heuristic mangles compound commands, shredding file content into sample_paths","description":"Hit repeatedly while doing conformance work on 2026-07-25. Not a blocker, but it costs a wasted round trip almost every time and will do the same to every future agent.\n\nzero.token.shell wraps some commands in a \"structured (repo inventory command)\" policy that reformats output into a repo_inventory block with files_seen/dirs_seen/sample_paths. That is useful for a bare ls. It is actively wrong when the command is a compound shell line whose real payload is on stdout.\n\nConcrete case:\n  zero.token.shell(\"ls /tmp/qs/probe-reports/ && cat /tmp/qs/probe-reports/$(ls ... | head -1)\")\nThe heuristic saw a leading ls, classified the whole thing as repo inventory, and then rendered the JSON report I actually wanted as \"sample_paths\" entries, one array element per LINE of JSON:\n  - gz-2026-07-25-050508.json\n  - \"contract_version\": \"1.0\",\n  - \"invalid CodeMode ref \\\"gz://...\\\"\",\n  - \"name\": \"ctx.step\",\nSo the file content was shredded into a path list. Every line got treated as a filesystem path. I had to re-fetch via the combined_ref to read it.\n\nSecond case, same root cause: an ls of two paths where the second does not exist returns exit 1 and the stderr line\n  ls: /path: No such file or directory\nappears as a sample_paths ENTRY, alongside real -rwxr-xr-x lines. A consumer parsing sample_paths as paths gets an error message as a path.\n\nSuggested fix, roughly in order of value:\n1. Only apply the inventory policy when the command is a SINGLE listing command with no &&, ||, ;, or | and no command substitution. A compound line should fall through to plain capture.\n2. Never route stderr into sample_paths. Inventory is a stdout concept.\n3. When the policy is applied, still make the raw stdout trivially reachable; today combined_ref works but it is a second round trip that the caller did not know they needed until they saw mangled output.\n\nImpact is mild but constant: it silently corrupts the shape of data you asked for, and it does so most often when an agent is reading evidence files, which is exactly when fidelity matters. Filed rather than worked around, per the standing instruction to report ZeroStack problems as beads.","status":"closed","priority":2,"issue_type":"bug","assignee":"AzureOrchid","created_at":"2026-07-25T09:11:37.576357Z","created_by":"aditya","updated_at":"2026-07-25T09:27:22.237352Z","closed_at":"2026-07-25T09:27:22.237311Z","close_reason":"Independently re-verified on the live origin-main binary TokenZero 78a9a12 from a fresh session. Compound commands with a leading listing are no longer classified as repo inventory and return the payload verbatim. Listing a real and a missing path still classifies as inventory but sample paths now contains only the real entry, with the stderr diagnostic excluded. Bare single listings are still classified as inventory as intended. Fixed in TokenZero 7bada5e.","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0,"comments":[{"id":1000008,"issue_id":"zerostack-je4","author":"QuietSummit","text":"VERIFIED LIVE on the rebuilt origin/main binary (TokenZero 78a9a12, built 05:25).\n\nBefore, the exact command that motivated this bead shredded the JSON body into sample_paths, one entry per line:\n  ls /tmp/qs/probe-reports/ && cat /tmp/qs/probe-reports/<f>\n  -> repo_inventory: sample_paths: - \"contract_version\": \"1.0\", - \"name\": \"ctx.step\", ...\n\nAfter, the same shape returns the payload verbatim:\n  ls /tmp/qs/probe2/ && cat /tmp/qs/probe2/<f> | head -20\n  -> gz-2026-07-25-050713.json\n     {\n       \"ns\": \"gz\",\n       \"bin\": \"graphzero-codemode + graphzero\",\n       ...\n  No repo_inventory wrapper, no sample_paths, content readable in one call.\n\nstderr fix also verified:\n  ls -la <real> <missing>\n  -> files_seen: 1, sample_paths contains ONLY the real entry.\n  The \"No such file or directory\" line no longer appears as a path.\n  Note that ls ... 2>&1 still shows the diagnostic, correctly: the redirect\n  merges the streams in the shell before the renderer ever sees them, so\n  there is nothing to separate at that point. Out of scope for this fix.\n\nFixed in TokenZero 7bada5e, on origin/main.\n\nCaveat worth recording: verifying this required manually fetching and rebuilding the live checkout, because the origin/main backend refresh is stuck. Filed as zerostack-9jj. Without that manual step a push does NOT reach the live surface, and I would have \"verified\" against a two-day-old binary.\n","created_at":"2026-07-25T09:26:05Z"}]}
{"id":"zerostack-ljx","title":"TokenZero shared store has no project namespacing: unrelated projects collide on one path","description":"This is the only divergence in the store-root triplicate that puts DIFFERENT objects at the SAME path, rather than the same object at different paths.\n\nTokenZero/crates/tokenzero-engine/src/workspace.rs:106-124 always resolves to <store>/tokenzero/recovery-cache.json with no project key. N unrelated projects sharing one ZEROSTACK_STORE_ROOT all write that same file. workspace.rs:203-216 only emits a store_project_mismatch warning in the resolution report; nothing prevents the collision.\n\nThe other two namespace correctly when the store is outside the repo:\n- GraphZero: <store>/projects/<project_key>/graphzero/ (zerostack_store.rs:114-123)\n- FSZero: <store>/fszero/repos/<sid>/ (zerostack_store.rs:56-62)\n\nRecommended canonical shape is GraphZeros: <root>/projects/<key>/<engine>, with the key being 16 lowercase hex chars of sha256 over the absolutized root path, no prefix. FSZeros sid- prefix belongs at the directory level, not in the key; keeping prefixes means no engine can predict another engines shard directory (FSZero sid-<16hex> = 20 chars vs GraphZero <16hex> = 16).\n\nAlso: key input bytes diverge. FSZero uses fs::canonicalize or the path as-given without absolutizing (zerostack_store.rs:100-105); GraphZero absolutizes against cwd when the path does not exist (store/path_safety.rs:35-45). For a nonexistent relative repo_root, FSZero hashes \"rel/path\" and GraphZero hashes \"<cwd>/rel/path\", producing different keys for the same repo.\n\nMigration: TokenZero gains namespacing, <pin>/tokenzero/recovery-cache.json moves to <pin>/projects/<key>/tokenzero/recovery-cache.json. FSZero moves fszero/repos/<sid>/ to projects/<key>/fszero/. GraphZero unchanged.\n\nAnti-collision gate: two distinct roots sharing one pin must resolve to different engine dirs. That test fails against TokenZero today.\n\nPart of zerostack-sce part B.","status":"open","priority":1,"issue_type":"bug","created_at":"2026-07-25T08:23:42.207979Z","created_by":"aditya","updated_at":"2026-07-25T10:11:02.120422Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-machine-permit-into-hub-1iz","title":"wave 1.4: move zerostack-machine-permit into hub crates (unblocks two engine p0s)","description":"zerostack-machine-permit is cross-engine shared infrastructure that currently lives inside TokenZero at TokenZero/crates/zerostack-machine-permit. Both other engines already depend on it from outside TokenZero, so the hub is its correct home.\n\nCurrent consumers:\n- FSZero/Cargo.toml:87  zerostack-machine-permit = { path = \"../tokenzero/crates/zerostack-machine-permit\" }\n- GraphZero/crates/graphzero-query/Cargo.toml:28  git dep on the tokenzero repo, rev 45e242cb63ede0dedd8e10e3f8f322fe8d360c20\n\nBUG found while surveying: the FSZero path dep spells the sibling directory \"tokenzero\" but the real directory is \"TokenZero\". It resolves only because macOS APFS is case-insensitive. On a case-sensitive filesystem (Linux CI, most containers) FSZero fails to build. Confirmed: ~/AI/tokenzero and ~/AI/TokenZero both stat successfully on this machine, and only TokenZero is the real entry.\n\nAlso cross-repo: GraphZero/crates/graphzero-test-support/Cargo.toml:23 path-deps ../../../TokenZero/crates/tokenzero-recovery, which is the other half of the graphzero-ml4r standalone-build failure.\n\nDeliverables:\n1. Move the crate to ZeroStack/crates/zerostack-machine-permit, add to the hub workspace members.\n2. Repoint FSZero and GraphZero to the hub via pinned git rev, matching how zero-abi and zero-ref are already pinned.\n3. Remove the TokenZero copy, or leave a re-export shim for one release if TokenZero-internal callers need it.\n4. Verify FSZero builds with a case-sensitive path check, not just on macOS.\n\nUnblocks: fszero-ixka (p0 standalone build), graphzero-ml4r (p0 standalone build).\nRelated: zerostack-hxl deliverable 5 (crates.io publish removes the git pin entirely).","status":"in_progress","priority":0,"issue_type":"task","assignee":"CobaltAnchor","created_at":"2026-07-25T07:09:06.153444Z","created_by":"aditya","updated_at":"2026-07-25T08:11:45.662428Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0,"comments":[{"id":1000001,"issue_id":"zerostack-machine-permit-into-hub-1iz","author":"aditya","text":"Progress, hub side landed and verified.\n\nDone:\n- crates/zerostack-machine-permit added to the hub, src copied byte-identical from TokenZero (diff -r clean, marker SRC_BYTE_IDENTICAL).\n- Manifest rewritten to hub convention: explicit version 0.1.0, edition.workspace, license.workspace, explicit description. TokenZero had inherited version/repository/homepage/readme/keywords/categories/rust-version from its own workspace, which the hub workspace does not define.\n- Added to hub workspace members.\n- Equality gate: 22/22 inline tests pass in the hub under edition 2021 stable, matching 22/22 in TokenZero under edition 2024 nightly. The crate is edition-agnostic. Hub suite went 38 to 60 tests.\n\n- FSZero repointed and verified. The case-sensitivity build break is fixed:\n    - was: zerostack-machine-permit = { path = \"../tokenzero/crates/zerostack-machine-permit\" }\n    - now: zerostack-machine-permit = { path = \"../ZeroStack/crates/zerostack-machine-permit\" }\n  This both fixes the Linux/case-sensitive-filesystem failure and removes the FSZero to TokenZero coupling. Verified: cargo metadata exit 0, cargo check -p zerostack-machine-permit compiles v0.1.0 from the hub path, Cargo.lock updated 1.4.0 to 0.1.0.\n\nDeliberately not done, and why:\n- GraphZero left untouched. It already uses a git dep on the tokenzero repo rev 45e242cb63ede0dedd8e10e3f8f322fe8d360c20, which is push-independent and works today. Converting it to a path dep would introduce a sibling-checkout requirement and make graphzero-ml4r worse. It should flip straight to a hub git dep.\n- TokenZero copy left in place. Removing it entangles this work with open PR #31 on branch adopt-zero-ref.\n\nBlocked on a push, not on code: FSZero and GraphZero can only take pinned git deps on the hub once this hub commit exists on the zerostack remote, the same way zero-abi and zero-ref are pinned at 90cdbfad5fa5bc5d601e841def2416d64810a487. No push authority in this session, so the flip is the remaining step.\n\nOpen risk while the flip is pending: two byte-identical copies of the crate exist, hub and TokenZero, with no guard against divergence. Worth a hub check that compares them, or just land the TokenZero removal promptly.\n\nNote the version goes backwards for anyone who was reading it, 1.4.0 in TokenZero to 0.1.0 in the hub, because the hub versions its foundation crates at 0.1.0. Harmless for path and rev-pinned git deps, but it needs a decision before any crates.io publish (zerostack-hxl deliverable 5).","created_at":"2026-07-25T07:45:49Z"},{"id":1000002,"issue_id":"zerostack-machine-permit-into-hub-1iz","author":"aditya","text":"Pushed. Hub rev d8903ed958867670ad1bd669e06e4c31e72b2c29 is on origin/main.\n\nLanded:\n- ZeroStack: two commits, bd4fbaf (retire bd for br, remove bd git hooks, document zero.* routing) and d8903ed (extract the crate). Pushed e3ef302..d8903ed.\n- TokenZero: PR #31 merged as e65e4c3, plus 048841b carrying the br-format beads export. Local main synced to origin.\n- FSZero: 4646af0, flipped to the pinned hub git dep. Verified cargo check resolves v0.1.0 from git+.../zerostack?rev=d8903ed and Cargo.lock records that source. Pushed 69cf154..4646af0. The case-sensitivity build break is gone, so a clean clone on Linux now works.\n\nRemaining, both deliberate:\n1. GraphZero not repointed. That checkout has 76 modified tracked files and 11 untracked, including a live refactor across crates/graphzero-cli. Editing its Cargo.toml or Cargo.lock would entangle this work with an in-flight session and risk pushing half-finished state. Sent Agent Mail message id 509 to AzurePelican and PurpleRiver in the GraphZero project with the exact one-line change for crates/graphzero-query/Cargo.toml:28.\n2. TokenZero copy of the crate still present, so two byte-identical copies exist with no divergence guard. Removing it is deliverable 3 and is now unblocked, since the hub rev is published.\n\nAlso removed as part of retiring bd: core.hooksPath pointed at .beads/hooks and pre-push ran bd hooks run pre-push, which would have written to the retired Dolt store on every push and re-created the divergence this session just fixed. The five tracked hook scripts are deleted and hooksPath is unset. The hub was the only repo with them.\n\nIncidentally dogfooded the extracted crate under real contention: a zero_execute call failed with machine_permit_busy on /tmp/zerostack-codemode-heavy-8a6d3b8d1b260a41.permit held by another live session, and succeeded on retry. Correct behavior, and it confirms multiple sessions are sharing the permit as designed.","created_at":"2026-07-25T08:11:45Z"}]}
{"id":"zerostack-mpi","title":"GraphZero execute_code envelope lacks execution_id and inline telemetry the contract requires","description":"Found by the first live conformance run after the harness was repaired (ZeroStack 2241b75, plus the payload-extraction fix). G2 refs, G3 telemetry, G6 ctx.step, and G9 coalescing all FAIL against GraphZero, and unlike G5/G10 these are NOT harness bugs.\n\nCONTRACT.md section on {ns}_execute_code specifies the success envelope:\n  { \"ack\": \"C\",\n    \"execution_id\": \"cm://exec/1782920000000-012345abcdef\",\n    \"refs\": { \"code\":..., \"steps\":..., \"telemetry\":..., \"result\":... },\n    \"telemetry\": { \"kind\": \"codemode.execute\", \"status\": \"ok\", \"logical_ops\":...,\n                   \"physical_ops\":..., \"wall_ms\":..., ... } }\n\nGraphZero actually returns (verified by direct stdio invocation of\ntarget/release/graphzero-codemode, plan \"return 1+1;\", form js):\n  structuredContent top-level keys: [\"ack\", \"ref\", \"refs\", \"value\"]\n  ack   = \"ok gz2 96% t:q:7149bee088e3c5a9\"\n  ref   = \"gz://codemode/execution/cm___exec_1784970487262-50629-0-aae1dac37219/result\"\n  refs  = [ gz://blob/<64hex>, .../<id>, .../<id>/steps, .../<id>/telemetry, .../<id>/result ]\n  value = { ack: \"C\", ref: \"gz://blob/...\", value: 2 }\n\nThree concrete divergences:\n\n1. NO execution_id field. The contract wants a top-level execution_id of the form cm://exec/<id>. GraphZero embeds the id only inside ref/refs strings, spelled cm___exec_1784970487262-50629-0-aae1dac37219 (triple underscore, not cm://exec/). A consumer cannot read the execution id without string-parsing a ref, and the id spelling itself differs from the contract's.\n\n2. NO inline telemetry object. The contract wants telemetry inline in the response. GraphZero returns only a gz://.../telemetry REF, requiring a second round trip. This is defensible as a ref-first design and may well be the better shape, but it is not what the contract says, and G3/G9 cannot pass as written. Note G9 coalescing specifically needs telemetry counters to observe batching, so a ref-only telemetry makes that check impossible without an expand step the harness does not do.\n\n3. ack is not \"C\". The contract shows ack \"C\" on success; GraphZero returns \"ok gz2 96% t:q:<hash>\" at the envelope level and puts ack \"C\" one level down in value. So the contract's shape appears at value, not at the top.\n\nNote GraphZero looks INTERNALLY consistent and arguably richer than the contract; this reads as the contract having been written against an older or different envelope and never reconciled, rather than as a regression. That is exactly the \"closed-but-unproven\" state zerostack-conformance-refresh-ci-30a flagged: GraphZero closed zeroref-v1-shared-cas-1ghi.8/.9 as done while its own evidence file stayed pending.\n\nNeeds a contract owner decision, same as zerostack-g25:\nA. The contract is normative and GraphZero must add execution_id and inline telemetry.\nB. The ref-first envelope is correct and CONTRACT.md plus the harness must be updated to match, with telemetry expansion added to G3/G9.\n\nDo not guess. Whichever way it goes, TokenZero and FSZero must be checked for the same divergence before a shared answer is written down, since the contract is supposed to be cross-engine.\n\nRelated: zerostack-hix (harness), zerostack-g25 (mutation vocabulary), zerostack-conformance-refresh-ci-30a (parent).","status":"open","priority":1,"issue_type":"bug","created_at":"2026-07-25T09:08:32.428912Z","created_by":"aditya","updated_at":"2026-07-25T09:08:32.428912Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-nqm","title":"RCH active-project exclusion silently falls back to local compilation","description":"Observed live 2026-07-25. RCH_VISIBILITY=verbose rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_zerostack cargo test -p zero-store --all-targets returned exit 0 but printed `[RCH] local (no admissible workers: active_project_exclusion=1)` and compiled locally while another ZeroStack clippy job occupied spark-1672. This violates remote-only build policy and can exhaust the Mac while looking successful. Deliverables: (1) add a strict remote-only mode or env that errors instead of fail-open; (2) pi/rch workflow uses strict mode for all builds/tests; (3) active-project exclusion should queue or return structured busy, not run locally; (4) regression test proves no local compiler process starts; (5) telemetry distinguishes remote success, queued/busy, and forbidden local fallback.","status":"open","priority":1,"issue_type":"task","created_at":"2026-07-25T10:07:45.317497Z","created_by":"aditya","updated_at":"2026-07-25T10:07:56.053604Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-oh7","title":"CAS temp-file reapers are mutually blind: each engine leaks the others temp files forever","description":"Three temp-file naming schemes and two mutually exclusive reap predicates, so abandoned temps from a crashed publisher are never cleaned by a different engine sharing the store.\n\nNames:\n- hub ZeroStack/crates/zero-store/src/cas.rs:197 and GraphZero .../shared_cas.rs:141 -> .tmp-<hash[..8]>-<pid>-<seq>\n- TokenZero .../shared_cas.rs:87 -> .tmp-<hash64>-<nanos>-<n>.blob\n- FSZero src/core/cas.rs:231-238 -> <hash64>.<pid>.<seq>.tmp\n\nReap predicates:\n- hub cas.rs:300-320 and GraphZero :251 match PREFIX \".tmp-\", age >= 3600s\n- FSZero cas.rs:281-291 matches SUFFIX \".tmp\", age >= 900s\n- TokenZero has no reaper at all\n\nSo FSZero never reaps hub or GraphZero or TokenZero temps (no .tmp suffix), and hub and GraphZero never reap FSZero temps (no .tmp- prefix). On a shared store root the leak is permanent and grows with every crashed publish.\n\nFix: one reaper in zero-store that recognizes all three historical shapes. Equality gate: a test that plants all three shapes and asserts the unified reaper removes them when stale and none of them when young.\n\nPart of zerostack-sce part A wave 1.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-07-25T08:23:42.272907Z","created_by":"aditya","updated_at":"2026-07-25T10:11:02.664514Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-p94","title":"uncommitted TokenZero duplicate of VISIBLE_BUDGET_LOSSY_DECLARATION will fail CI and reopens tokenzero-rnv2","description":"Found while investigating zerostack-spz part 1. Reported by Agent Mail with ack requested, filed as a bead so it survives an unread inbox.\n\nWHERE\ncrates/tokenzero-core/src/tokens.rs:37, UNCOMMITTED, on branch harness-no-host-paths, in another session's working tree (81 files dirty at the time of filing).\n\nWHAT\n  const LOSSY_VISIBLE_BUDGET_MARKER: &str = \"[mode=lossy lossy_policy_id=tokenzero.visible-compression.v1 lossy_spans=[{description=omitted-bytes reason=visible-budget recovery_may_be_needed=true}]]\";\nIt is a byte-identical duplicate of pub const VISIBLE_BUDGET_LOSSY_DECLARATION two lines above it, at line 35, and it has zero callers. grep across the workspace returns exactly one hit: the definition.\n\nIMPACT\n  cargo build -p tokenzero-core\n  warning: constant `LOSSY_VISIBLE_BUDGET_MARKER` is never used\n  warning: `tokenzero-core` (lib) generated 1 warning\nTokenZero CI runs clippy with -D warnings, so this becomes a hard build failure the moment it is committed.\n\nWHY IT IS A REGRESSION AND NOT MERELY DEAD CODE\ntokenzero-rnv2 was closed for fixing exactly this. Its fix (5d82ea2) collapsed three duplicated copies of this string into a single pub const, because a divergent copy was the root cause of the visible-budget test failing invariant P01-001: the test modelled a 14-token marker while the emitted marker costs 33, so with budget=17 the marker alone exceeded the budget and only the footer could be emitted.\n\nVerified origin/main is clean: it contains only the declaration at line 35 and its single use at line 51, with no second constant. So the duplicate was reintroduced locally AFTER the fix landed. If it is committed, rnv2 reopens, and the failure mode it reintroduces is the metadata displacing the payload it describes.\n\nFIX\nDelete line 37. Do not wire anything to it. Every call site should keep using VISIBLE_BUDGET_LOSSY_DECLARATION, which is documented in place as the single source of truth precisely to prevent this.\n\nNOT DONE BY ME, deliberately: the file is another session's uncommitted work, and editing an 81-file dirty tree underneath a live session risks destroying it. That already happened once today in this repo (zerostack-wth). Owner should delete it before committing.\n\nAlso blocks zerostack-spz part 1, whose fix lives in enforce_token_budget_with_ref in this same file.ZEOF\nwc -c /tmp/az/dup.txt","status":"open","priority":1,"issue_type":"bug","created_at":"2026-07-25T10:07:42.990303Z","created_by":"aditya","updated_at":"2026-07-25T10:07:42.990303Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-pi1","title":"store-root resolution: FSZero inverts precedence and has no shared-store opt-in gate","description":"Two engines resolve the same repo to different store roots, so a single logical store silently splits.\n\nFSZero/src/core/zerostack_store.rs:20-27 checks the env pin FIRST and has no opt-in gate:\n  if let Some(v) = env(ZEROSTACK_STORE_ROOT).or(ZERO_STACK_STORE_ROOT) { return Some(...) }\n  let candidate = repo_root.join(\".zerostack\"); if candidate.is_dir() { Some(candidate) } else { None }\n\nTokenZero/crates/tokenzero-engine/src/workspace.rs:77-95 and GraphZero/crates/graphzero-store/src/store/zerostack_store.rs:91-112 both check local .zerostack FIRST, then require a shared-store opt-in before honoring the pin.\n\nConsequences:\n- Repo with .zerostack present plus ZEROSTACK_STORE_ROOT=/shared and no opt-in: FSZero writes /shared/fszero/repos/<sid>/, TokenZero and GraphZero write <repo>/.zerostack/.\n- Bare ZEROSTACK_STORE_ROOT with no .zerostack: FSZero honors it, the other two ignore it.\n- FSZERO_SHARED_STORE does not exist anywhere in FSZero source. Only a doc mention of ZEROSTACK_SHARED_STORE at FSZero/docs/design/zeroref-v1-annex.md:152. Adding it is a new surface, not a rename.\n\nRecommended canonical behavior, 2 of 3 majority: local .zerostack wins over the pin, and the pin requires opt-in. The local marker is an explicit per-repo declaration; a process-global env is ambient and a stray value in an agent harness should not relocate one engines store.\n\nMigration: FSZero only. Repos with both .zerostack and a pin move <pin>/fszero/{store.sqlite3,repos/<sid>/store.sqlite3} to <repo>/.zerostack/fszero/store.sqlite3. FSZero already has the machinery at src/core/store_migration.rs:87-100. With a bare ungated pin, FSZero returns to <repo>/.fszero/store.sqlite3, which looks like data loss to existing users, so ship FSZERO_SHARED_STORE plus a one-shot migration that adopts the old ungated location.\n\nPart of zerostack-sce part B.","status":"open","priority":1,"issue_type":"bug","created_at":"2026-07-25T08:23:42.145365Z","created_by":"aditya","updated_at":"2026-07-25T10:11:02.215314Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-retire-legacy-dolt-stores-emq","title":"retire legacy .beads/embeddeddolt in all four repos after the br migration","description":"Follow-up from the completed bd to br tracker migration.\n\nAll four repos now use br with SQLite at .beads/beads.db, and .beads/issues.jsonl is the br-written export. The old bd Dolt data still sits in .beads/embeddeddolt in every repo and is now a divergence trap: anyone who runs bd writes a second store that br cannot import, because bd emits comments[].id as a string where br requires an integer.\n\nMigration outcome for the record, br is authoritative and no br content was overwritten:\n- TokenZero 324 issues total, 19 recovered from Dolt that br had never seen\n- FSZero 366 total, 12 recovered\n- GraphZero 843 total, 8 recovered\n- ZeroStack 7 total, SQLite bootstrapped from scratch, 1 UUIDv7 comment id normalized\n\nPre-migration backups are at /tmp/beads-backup-premigrate-20260725-<repo>. Those are temp-dir copies and will not survive a reboot.\n\nDeliverables:\n1. Move the backups somewhere durable, or confirm they are disposable.\n2. Delete .beads/embeddeddolt in all four repos.\n3. Confirm no CI job, git hook, or agent doc still invokes bd.","status":"closed","priority":3,"issue_type":"task","assignee":"AzureOrchid","created_at":"2026-07-25T07:09:06.486149Z","created_by":"aditya","updated_at":"2026-07-25T10:00:52.281550Z","closed_at":"2026-07-25T10:00:52.281503Z","close_reason":"All three deliverables done. Backups moved to ~/AI/archives/beads-premigrate-20260725 (357M, verified by size and file count). embeddeddolt deleted in all four repos (141M) after verifying br totals meet or exceed the post-migration counts, and re-verified identical after deletion. No hook, CI job, or doc invokes bd. Beyond the ask: FSZero was the one repo with nothing ignoring the store, fixed in the tracked root gitignore so it survives a clone (FSZero 0515728, pushed). bd is still installed on PATH; uninstalling needs the user.","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-rhd","title":"CRITICAL TOCTOU: CAS publish never takes the GC coordination lock, so GC unlinks live objects","description":"A publisher and the garbage collector can interleave such that GC unlinks an object a publisher just republished or is about to reference. Publish returns Ok(hash), contains(hash) was true, and the object file is then gone. Later reads are NotFound, not Corruption, so this is silent data loss plus a dangling reference from a committed root. Nothing panics and the GC report claims a clean Collect.\n\nRoot cause: publish takes no coordination lock in any engine, and the sweepers pre-unlink recheck inspects only gc/** metadata, never the object identity or mtime.\n\nTokenZero/crates/tokenzero-recovery/src/shared_cas.rs:\n  :77-109   publish() - no lock acquired\n  :1134     run_gc() acquires GcCoordLock\n  :1213     per-hash recheck: load_mark_state again (metadata only)\n  :1217     cas.remove_object(hash) -> unlink\n  :178-201  remove_object re-stats only \"regular file, not symlink\"\n  :1079-1092 GcCoordLock::acquire, taken by run_gc and the three record publishers, never by publish\n\nInterleaving (GC left, publisher right):\n  1 acquire lock\n  2 load_mark_state: h absent from roots/pins/leases, uncertain=false\n  3 build_dry_run_report reads h mtime -> age >= min_age -> Collect\n  4 write report, persist progress\n  5                         publish(bytes) for the same h, no lock. dest exists -> verify_existing -> Ok(h). No mtime touch, no pin, no lease.\n  6                         caller records h into an in-flight root/manifest; new snapshot not yet published\n  7 recheck load_mark_state: still no reference -> passes\n  8 remove_object(h) -> unlink\n  9                         publisher commits the root referencing h\n  11                        later resolve(h) -> NotFound\n\nVariant B: a fresh publish lands a new inode via create_new + rename for a hash already listed Collect, and step 8 deletes it. min_age is defeated twice: the age came from the pre-rename or pre-republish inode, and remove_object never re-stats mtime.\n\nPer-engine:\n- TokenZero: vulnerable and is the deleter. Publish lockless, recheck metadata-only, unlink without identity or mtime recheck and without quarantine.\n- FSZero: vulnerable, partially mitigated. It holds an exclusive advisory lock for the whole sweep (src/core/cas.rs:249-251) but put/put_prehashed never acquire it (:146-188), so its own window is identical. touch_blob on existing-put and on get (:161,:197) plus a 7-day grace (:53) reduce exposure but do not close it: a blob older than the grace that gains a new reference without any put or get is still deleted, and TokenZero GC ignores mtime once min_age is satisfied.\n- GraphZero: cannot cause it, never deletes (crates/graphzero-store/src/store/gc_roots.rs:11-13), but is a victim. SharedCas::put takes no lock, so TokenZero run_gc on a shared root can unlink a GraphZero object.\n- No engine mitigates it fully.\n\nFix, once, in the hub: one advisory lock at <store_root>/gc/coordinator.lock. Publishers take it Shared across hash -> dest check -> rename -> protection record write; the sweeper takes it Exclusive across mark -> report -> recheck -> quarantine/unlink. remove_object and quarantine_object accept a &StoreLock rather than acquiring one, so the sweeper cannot forget it. Single lock means no lock order and no cycle; both acquisitions are deadline-bounded; kernel releases on process death so no stale reclaim is needed.\n\nRegression test must be deterministic via a GcHooks::before_unlink seam plus channels, not timing: block GC between recheck and unlink, then assert a concurrent protected publish blocks (proof the shared lock is excluded), release, and assert get_verified returns the bytes. Pre-fix behavior is the exact inverse.\n\nImplemented as part of zerostack-sce wave 1.","status":"open","priority":0,"issue_type":"bug","created_at":"2026-07-25T08:23:42.075656Z","created_by":"aditya","updated_at":"2026-07-25T10:11:02.019355Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-sce","title":"wave 1.5: CAS publish/GC core + .zerostack store-root resolution into zero-store","description":"Two drift-forbidden pieces move into zero-store. (A) CAS internals: TZ shared_cas.rs (1,485 LOC incl. zerostack.cas-gc.v1 GC), FZ core/cas.rs (341), GZ store CAS — shared publish protocol (temp+fsync+rename), object verify, and the GC state machine live once; fixes the omega-math CRITICAL TOCTOU (GC unlinking concurrently republished objects; publish never takes the GC lock) exactly once instead of three times. (B) Store-root resolution: TZ engine/workspace.rs (~250), FZ core/zerostack_store.rs (137), GZ store/zerostack_store.rs (533) all reimplement .zerostack/ detection, ZEROSTACK_STORE_ROOT/ZERO_STACK_STORE_ROOT precedence, shared-store opt-in, and sha256 project-key namespacing — if these drift, engines put data in different places and interop silently breaks. Shared module owns layout + resolution; engines keep alias envs (TOKENZERO_SHARED_STORE etc.) as thin wrappers. TZ = PR, GZ/FZ direct. Equality gates: digest byte-identical per engine + store-path golden tests (same inputs resolve same paths before/after).","status":"in_progress","priority":1,"issue_type":"task","assignee":"aditya","owner":"adityavgcode@gmail.com","created_at":"2026-07-24T09:20:37Z","created_by":"AdityaVG13","updated_at":"2026-07-25T08:35:24.471336Z","source_repo":".","compaction_level":0,"original_size":0,"comments":[{"id":1000003,"issue_id":"zerostack-sce","author":"aditya","text":"Wave 1 part B landed, plus the publish side of part A.\n\nShipped in crates/zero-store:\n- store_root.rs: one resolver for all three engines. Canonical rules, reconciling the three that had drifted: project-local .zerostack outranks the env pin; the pin requires shared-store opt-in; an exported-but-empty pin means unset; a pin outside the project is namespaced as <root>/projects/<key>/<engine>; the project key is 16 lowercase hex of sha256 over the absolutized root path with no prefix; the CAS is shared per store root and never project-namespaced. Engines pass only their own opt-in aliases, so precedence and truthiness live in one place. 21 tests, driven from the golden matrix, including the two gates the engines fail today: distinct projects never collide under one pin (TokenZero fails this, zerostack-ljx) and all three engines derive the same project key (FSZero fails this on its sid- prefix).\n- gc_lock.rs: the single store coordination lock at <store_root>/gc/coordinator.lock. Publishers shared, sweeper exclusive. One lock means no lock order and no cycle, so deadlock freedom is structural. Acquisition is deadline-bounded so a wedged holder surfaces as a typed WouldBlock rather than a hang, and the kernel releases on process death so no stale reclaim is needed. The path is deliberately identical to TokenZeros existing GcCoordLock, and both fs4 and std map to flock on Unix, so a hub publisher and a not-yet-migrated TokenZero sweeper interoperate during the cutover.\n- cas.rs: publish now runs under the shared guard, which is the publish half of the TOCTOU fix (zerostack-rhd). Added PutOutcome with a created flag, put_outcome, put_prehashed that re-derives and compares the digest so a wrong hash writes nothing, touch, list_objects, and remove_object plus quarantine_object which take a &StoreLock instead of acquiring one, so a sweeper cannot forget the guard. Dedup now refreshes mtime, since a dedup is a fresh reference and an age-based policy that never sees it collects a live object. The temp reaper recognizes all three historical engine shapes, closing the mutual blindness in zerostack-oh7. object_path no longer panics on short input.\n\nVerification: zero-store 8 tests to 45. Workspace 60 to 100, all passing. zero-store is clippy-clean and fmt-clean. The publish/GC race has a deterministic regression test that parks a sweeper between its decision and its unlink using channel rendezvous, asserts a publish is excluded, then asserts the republished object is readable. No timing assumptions anywhere.\n\nEmpirically resolved an open question from the design analysis: on rustc 1.97.1 the std signatures are File::try_lock() -> Result<(), std::fs::TryLockError> and try_lock_shared() the same, not Result<bool>. Probed rather than assumed, so no new dependency was needed: fs4 is not required.\n\nRemaining for this bead, the larger half of part A: port the zerostack.cas-gc.v1 state machine itself (records, mark, report, resumable sweep, epoch monotonicity, uncertainty policy) from TokenZero shared_cas.rs:296-1471 into a zero-store gc module, which needs serde promoted from dev-dependency to dependency. Until that lands the lock is in place but the sweeper still lives in TokenZero. Then the engine cutovers: TokenZero by PR, FSZero and GraphZero direct.\n\nAlso filed from the analysis of the three implementations: zerostack-rhd, pi1, ljx, oh7, 2x7 in the hub, tokenzero-8tdg, 4xvw, mivh, fszero-1woo, 44nj, p0cz, graphzero-m94u, f2jn.","created_at":"2026-07-25T08:35:24Z"}]}
{"id":"zerostack-sg3","title":"br stamps source_repo_path with an absolute host path into the tracked .beads/issues.jsonl","description":"br (beads_rust) writes \"source_repo_path\":\"~/AI/ZeroStack\" into every issue record in .beads/issues.jsonl, which is tracked in this public hub repo. This is systemic, not a one-off: every issue carries it. The same applies to the TokenZero, FSZero, and GraphZero repos.\n\nThis is a different problem from the benchmark harness leak (zerostack-9g4): the writer is br itself, not a Python harness, so the fix is either:\n  a. Configure br to omit or relativize source_repo_path (check br config/help for an option), or\n  b. Add a pre-commit or pre-sync scrub that rewrites source_repo_path to the repo name or to \".\" in .beads/issues.jsonl before it is staged.\n\nEither way, new exports must stop carrying the absolute path. The already-committed content is accepted as-is (no history rewrite, per user decision in zerostack-9g4).\n\nIf br cannot be configured, the scrub script should be shared across all four repos since they all use br. Consider putting it in the hub scripts/ and referencing it from each repos pre-sync step.\n\nDo not untrack .beads/issues.jsonl: it is the cross-session bead handoff mechanism.\n\nRefs zerostack-9g4.","status":"closed","priority":2,"issue_type":"bug","assignee":"AzureOrchid","created_at":"2026-07-25T09:02:23.128037Z","created_by":"aditya","updated_at":"2026-07-25T09:57:03.550393Z","closed_at":"2026-07-25T09:57:03.550359Z","close_reason":"Fixed and pushed as ZeroStack eb2b7b7. br 0.2.16 has no config knob for source_repo_path (verified against br config list and br schema), so the shared scrub in scripts/scrub_beads_export.py is the fix. Gates de-allowlisted and wired into CI, both proven to fail on a reintroduced leak. Hub export scrubbed with integrity verified. Cross-repo application filed as zerostack-a9c.","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-spz","title":"lossy elision is only flagged in a trailing footer, never inline at the cut point","description":"Two smaller ZeroStack surface issues found during conformance work on 2026-07-25, filed together since both are about output fidelity.\n\n1. LOSSY COMPRESSION CAN DROP THE PAYLOAD WITH ONLY A FOOTER AS WARNING\nReading a conformance report through zero.token.shell returned a result tagged\n  [mode=lossy lossy_policy_id=tokenzero.visible-compression.v1\n   lossy_spans=[{description=omitted-bytes reason=visible-budget\n   recovery_may_be_needed=true}]]\nThe visible text was truncated mid-value. The footer is honest, but it is at the END of a long block, so an agent that reads top-down and stops early will act on a partial JSON payload believing it is complete. When the omitted span falls inside a JSON document the remaining text is not even parseable, yet nothing marks the truncation point inline.\nSuggested: mark the elision INLINE at the point it happens, e.g. a clear sentinel where bytes were dropped, so truncation is visible exactly where it matters rather than only in a trailing annotation. Bonus: when the payload is detected as JSON, prefer eliding whole values over cutting mid-token.\n\n2. grep -A/-B THROUGH shell RETURNS A REF, NOT THE MATCH\n  zero.token.shell(\"grep -n 'fn extract_json_payload' -A 30 file.rs\")\nreturned only\n  preview: 884:fn extract_json_payload(\ni.e. the first matched line, with the -A 30 context available solely behind a second expand of the blob ref. Same for -B. The whole point of -A/-B is the surrounding context, so the one flag that makes grep useful is the one whose output does not survive. This turns a routine one-call code read into two calls, every time.\nSuggested: when the command carries -A/-B/-C, treat the context lines as the payload and show them, since the caller has explicitly declared how much context they want.\n\nNeither is a correctness bug in the engines; both are ZeroStack surface ergonomics that cost an extra round trip and, in case 1, risk silent misreads. Filed per the standing instruction to report ZeroStack problems as beads.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-07-25T09:11:54.022416Z","created_by":"aditya","updated_at":"2026-07-25T10:07:27.991957Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0,"comments":[{"id":1000009,"issue_id":"zerostack-spz","author":"QuietSummit","text":"Part 2 (grep -A/-B context) FIXED and verified live on the rebuilt origin/main binary (TokenZero 78a9a12).\n\nBefore, the -A context was discarded and only the match line survived:\n  grep -n \"fn extract_json_payload\" -A 30 conformance/src/lib.rs\n  -> preview: 884:fn extract_json_payload(\n  The 30 lines of requested context were reachable only through a second expand of the blob ref.\n\nAfter, the context comes back inline in one call:\n  grep -n \"fn substrate_label\" -A 12 conformance/src/lib.rs\n  -> 498:fn substrate_label(config: &RunConfig) -> String {\n     499-    let name = |p: &PathBuf| {\n     500-        p.file_name()\n     ... through 509-}\n  Full requested window, no second round trip.\n\nFix: search_shell_view capped sample_matches at 20 unconditionally. It now scales the cap with the largest -A/-B/-C value (handling both -A30 and -A 30) and stays at 20 when no context was requested, so ordinary greps are unaffected. TokenZero 7bada5e.\n\nPart 1 (lossy elision only flagged in a trailing footer) is NOT fixed here. It turns out to have a matching RED test on the TokenZero side, tokens::tokenizer_tests::visible_budget_prefix_retains_every_fitting_line, which fails with a visible output consisting of nothing but the lossy footer: the compressor spent the whole budget on its own annotation and kept none of the payload, violating invariant P01-001. That is the same defect seen from the inside. Tracking it there as tokenzero-rnv2, since the fix belongs in the token budget accounting rather than in render/domain.rs.\n\nLeaving this bead open for part 1, now narrowed to: mark elision INLINE at the point bytes are dropped, and prefer eliding whole JSON values over cutting mid-token.\n","created_at":"2026-07-25T09:26:22Z"},{"id":1000012,"issue_id":"zerostack-spz","author":"AzureOrchid","text":"Part 1 is BLOCKED on a dirty TokenZero tree, not on design. Recording findings so it is not lost.\n\nBLOCKER\nThe fix belongs in enforce_token_budget_with_ref in crates/tokenzero-core/src/tokens.rs, and that file is uncommitted work belonging to another live session: 81 files dirty on branch harness-no-host-paths, with tokens.rs and lib.rs both modified. Editing underneath that risks destroying their work, which already happened to me once today in this repo (zerostack-wth). Mailed QuietSummit and CobaltAnchor with an ack request; will take this as soon as the tree is committed or stashed.\n\nSEPARATE REGRESSION FOUND WHILE INVESTIGATING, reported by mail\ncrates/tokenzero-core/src/tokens.rs:37 in that uncommitted work reintroduces\n  const LOSSY_VISIBLE_BUDGET_MARKER: &str = \"[mode=lossy ...]\";\na byte-identical, zero-caller duplicate of VISIBLE_BUDGET_LOSSY_DECLARATION on line 35. cargo build warns \"constant LOSSY_VISIBLE_BUDGET_MARKER is never used\", and TokenZero CI runs clippy with -D warnings, so it is a build failure once committed.\n\nIt is specifically a regression of tokenzero-rnv2, which was closed for collapsing three duplicate copies of this exact string into one pub const. Verified origin/main is clean: only the declaration at line 35 and its use at line 51, no second constant. The duplicate is local-only. If it lands, rnv2 reopens, since a divergent copy of this string is precisely what made the visible-budget test fail P01-001 (test modelled a 14-token marker, real marker costs 33).\n\nCURRENT BEHAVIOUR, read from the code\nenforce_token_budget_with_ref keeps a prefix of whole lines, then appends the marker as a trailing line:\n  out = prefix + \"\\n\" + marker\nIt already does the thing rnv2 added: marker_tokens is subtracted from the budget before deciding what fits, and if the marker alone exceeds the budget, or nothing fits, the marker is returned alone as a correctness floor. So the accounting is sound. The defect in this bead is purely POSITIONAL: the annotation is always appended at the end, never placed where bytes were actually dropped.\n\nNote the marker has two forms. With a recovery_ref it is a readable \"... omitted by visible budget; expand REF ...\" line; without one it is the machine-readable lossy declaration. Both are appended trailing, so both have this problem.\n\nDESIGN INTENDED, stated for objection before I start\n1. Emit the omission marker INLINE at the cut point rather than only as a trailing footer, so truncation is visible exactly where it happens to a top-down reader.\n2. When the payload is detected as JSON, elide whole values instead of cutting mid-token, since a mid-token cut leaves text that will not parse.\n3. Keep VISIBLE_BUDGET_LOSSY_DECLARATION as the single source of truth and preserve the marker-cost accounting rnv2 established, so P01-001 stays green.\nSince truncation here is always a suffix cut, \"inline at the cut point\" and \"trailing\" coincide for the plain case; the real win is item 2 plus not losing the marker when a caller reads only the head of a long block.ZEOF\nwc -c /tmp/az/spz.txt\n","created_at":"2026-07-25T10:07:27Z"}]}
{"id":"zerostack-t76","title":"migrate pack manifest to zero-abi canonical JSON (digest+signature invalidating)","description":"Follow-up to zerostack-graphzero-canonical-json-drift-881, which PROVED the divergence (GraphZero 41170ab, 4 passing differential tests) but deliberately did not migrate.\n\nGraphZero PackManifest::unsigned_canonical_json emits struct declaration order; zero_abi::canonical_json emits sorted keys. Different bytes, different SHA-256.\n\nMigrating is not a cleanup because unsigned_canonical_json is BOTH the digest input (digest_sha256) and the exact ed25519-signed payload (graphzero-pack/src/sign.rs:9 sign, :16 verify). Switching encoders changes every pack manifest digest and invalidates every existing signature.\n\nRequired:\n1. Bump MANIFEST_SCHEMA_VERSION so old and new manifests are distinguishable rather than silently incompatible.\n2. Decide verification policy for already-signed v1 manifests: accept via the legacy encoder, or hard-fail and re-sign. Do not silently accept both, that reintroduces the ambiguity.\n3. Delete unsigned_canonical_json, call zero_abi::canonical_json, promote zero-abi from dev-dependency to a real dependency of graphzero-pack.\n4. Flip the two intentionally-inverted assertions in canonical_json_drift.rs (they are written to fail once the encoders converge, with messages saying so).\n5. Record which manifests were hashed with the divergent encoder (deliverable 3 of the parent bead).\n\nDo NOT start this until deliverable 4 of the parent (hub-side guard) lands, or a third implementation can appear during the migration.","status":"open","priority":2,"issue_type":"task","created_at":"2026-07-25T09:00:53.427484Z","created_by":"aditya","updated_at":"2026-07-25T09:00:53.427484Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-vpa","title":"untracked development-contract.yml would re-enable auto CI in all 4 repos if ever committed","description":"development-contract.yml exists UNTRACKED in all four repos (ZeroStack, TokenZero, FSZero, GraphZero), byte-identical, and shipped with automatic triggers:\n\n  on:\n    pull_request:\n    push:\n      branches: [main]\n\n  jobs:\n    portable-xtask:\n      strategy:\n        matrix:\n          os: [ubuntu-latest, macos-latest, windows-latest]\n\nIt is NOT gitignored:\n  git check-ignore -v .github/workflows/development-contract.yml -> rc=1 in all four repos\n\nSo a single `git add -A` or `git add .github` in any of the four repos would start auto-running a 3-OS matrix on every push and every PR, four times over. That directly contradicts the owner directive that automatic CI stay off to preserve the GitHub Actions budget.\n\nCurrent state: I rewrote all four local copies to `on: workflow_dispatch:` but deliberately did NOT commit them. They are another session's in-flight work and not mine to land; committing someone else's unfinished workflow to main is worse than leaving it staged-safe.\n\nTracked workflows are already clean as of 59b7e19. Verified every workflow in all four repos now has exactly one trigger:\n  zerostack:  ci.yml [workflow_dispatch]\n  tokenzero:  ci.yml, release.yml, development-contract.yml [workflow_dispatch]\n  fszero:     ncib-gates.yml, development-contract.yml [workflow_dispatch]\n  graphzero:  rust-ci.yml, graphzero-claims.yml, development-contract.yml [workflow_dispatch]\n\nAcceptance criteria:\ndevelopment-contract.yml is either (a) committed in each repo with workflow_dispatch as its only trigger, or (b) added to .gitignore if it is meant to remain a local-only harness. Either way it must be impossible to accidentally commit a version with push/pull_request triggers.","status":"open","priority":1,"issue_type":"bug","created_at":"2026-07-25T10:03:56.481282Z","created_by":"aditya","updated_at":"2026-07-25T10:03:56.481282Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-wth","title":"repo-inventory view silently drops every listing entry without a dot or slash","description":"The repo-inventory view SILENTLY DISCARDS every output line that does not look like a file path. Entries are not counted, not sampled, and not mentioned anywhere. The agent sees a confident, well-formed inventory block that is simply missing most of the listing.\\n\\nREPRODUCTION (live origin/main binary, TokenZero 78a9a12):\\n\\n  mkdir -p /tmp/az/inv/alpha /tmp/az/inv/beta /tmp/az/inv/gamma\\n  touch /tmp/az/inv/notes.txt\\n  zero.token.shell(\"ls /tmp/az/inv; ls /tmp/az/DOES_NOT_EXIST\")\\n\\nActual visible output:\\n\\n  repo_inventory:\\n  command: ls /tmp/az/inv; ls /tmp/az/DOES_NOT_EXIST\\n  files_seen: 1\\n  dirs_seen: 0\\n  sample_paths:\\n  - notes.txt\\n  exit: 1\\n\\nalpha, beta and gamma are GONE. Not in sample_paths, not in dirs_seen, not in\\nfiles_seen, not reflected in any count. Three of the four entries in the\\ndirectory vanished, and nothing in the output hints that anything was dropped.\\nThere is no truncation marker because the renderer does not believe it\\ntruncated anything.\\n\\nI hit this for real before reducing it: \"ls ~/AI/pi; ls ~/.pi; ls ~/.pi/extensions\"\\nreturned a single sample_paths entry, agent.bak-clean-20260721-185218, because\\nthat was the only name in the whole combined listing containing a dot. I\\nconcluded the extensions directory was empty. It is not.\\n\\nROOT CAUSE\\n\\ncrates/tokenzero-core/src/render/domain.rs, inventory_stats:\\n\\n  for line in output.lines().map(str::trim) {\\n      if line.is_empty() || starts_with(\"===\") || starts_with(\"---\") { continue; }\\n      if line.ends_with('/') {\\n          stats.dirs += 1;\\n      } else if is_file(line) {\\n          stats.files += 1;\\n          if stats.paths.len() < sample_limit { stats.paths.push(line); }\\n      }\\n      ...\\n  }\\n\\nThere is no else. A line that neither ends in '/' nor satisfies is_file falls\\nthrough both arms and is dropped without a trace.\\n\\nThe predicate passed by repo_inventory_view is\\n\\n  |line| line.contains('/') || line.contains('.')\\n\\nso for the extremely common case of a bare \"ls dir\" (no -la, no trailing\\nslashes, relative names), ANY entry without a dot in its name is discarded.\\nThat is most directory names in most repos: src, crates, target, docs, scripts.\\nPlain files without extensions are lost too: README, Makefile, LICENSE.\\n\\nThe compact path has the same shape. render/noise.rs compact_repo_inventory_view\\ncalls inventory_stats with looks_like_inventory_file_path, which requires a\\nslash, a backslash, or a dot in the basename, so it drops dotless entries in\\nexactly the same way. Both entry points are affected.\\n\\nWHY THIS IS WORSE THAN THE USUAL ELISION\\n\\nEvery other lossy surface in TokenZero announces itself: a ref, a lossy footer,\\nan \"omitted N lines\" marker. This one does not. files_seen: 1 is a positive\\nfactual claim about the listing, and it is wrong. An agent has no reason to\\ndouble-check a count that the renderer just handed it, so the failure is silent\\nand the wrong conclusion is confidently drawn. This is the same class of trap as\\nzerostack-9jj, where a stale binary silently invalidates a verification.\\n\\nNOTE ON SCOPE\\n\\nThis is NOT the bug fixed by zerostack-je4 / TokenZero 7bada5e. That fix\\ncorrected WHICH commands get classified as inventory, by removing cat from\\nINVENTORY_COMMANDS and keeping stderr out of the view. It is verified working.\\nThis bead is about what the inventory renderer does to output once a command IS\\nlegitimately classified, and it predates that fix.\\n\\nSUGGESTED FIX\\n\\nNothing may be dropped without a trace. Count every non-empty, non-separator\\nline. Where a line cannot be classified as file or dir, still count it and still\\nmake it eligible for the sample, since for ls output it is an entry regardless.\\nKeeping the output cheap when the classification is clean matters, so a\\nreasonable shape is to report the unclassified count only when it is nonzero, so\\nwell-behaved listings are byte-identical to today and only the lossy case grows.\\n","status":"closed","priority":1,"issue_type":"bug","assignee":"AzureOrchid","created_at":"2026-07-25T09:31:13.195888Z","created_by":"aditya","updated_at":"2026-07-25T09:53:11.382935Z","closed_at":"2026-07-25T09:53:11.382888Z","close_reason":"Fixed and pushed as TokenZero 04fc43a. inventory_stats now counts unclassified lines into an other bucket and samples them, so no listing entry is dropped without a trace; both entry points covered. Proven RED before GREEN; 88/88 lib tests pass on a clean origin/main worktree via rch on the spark. See comment for evidence.","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0}
{"id":"zerostack-xyk","title":"multi-session git coordination: no protocol for shared working trees, duplicate work and near-miss data loss already occurred","description":"Multiple agent sessions (QuietSummit, AzureOrchid, CobaltAnchor) share the SAME working trees for all four repos. There is no protocol, and we are colliding.\n\nEVIDENCE\n\nDuplicate work already happened. tokenzero-b49m was claimed and fixed by me while 196d296 had already fixed half of it on origin/main a day earlier. The bead was still open, so nothing signalled it was done. Cost: one wasted cycle rediscovering a documented trap. It was luck that the two fixes were complementary (Makefile vs release.yml) rather than conflicting edits to the same file.\n\nShared trees carry large unlanded state, which is what makes any destructive command catastrophic:\n  tokenzero  branch harness-no-host-paths, 8 ahead / 2 behind origin/main, 81 dirty, 20 stashes, 2 worktrees\n  fszero     16 dirty, 21 stashes, 12 worktrees\n  graphzero  86 dirty,  6 stashes,  3 worktrees\n  zerostack   1 dirty,  0 stashes,  1 worktree\n\nRisky operations I performed myself in a shared tree (no git clean, but close enough to matter):\n  - `git stash push -u` then `git checkout -B` on harness-no-host-paths, a branch I did not own. The -u swept up untracked files belonging to other sessions. It restored intact only because the checkout aborted on a conflict; had it succeeded I could have stranded another session's work in a stash they would never look in.\n  - `git rebase`, which printed \"Created autostash\" - it silently stashed and reapplied another session's dirty tree.\n\nPROTOCOL (proposed, mailed to all sessions under thread push-coordination)\n\nClaiming:\n  - br update <id> --status in_progress --assignee <name> BEFORE starting.\n  - Also check `git log --oneline -20 --all --grep=<keyword>`. An open bead does NOT mean the work is undone.\n  - Fixed something without a bead? File and close it in the same push.\n\nPushing:\n  - Small, often, rebased onto main. Do not accumulate commits on private branches; unpushed work is invisible and causes duplicate effort.\n  - Pull/rebase before starting an item, not only before pushing.\n  - One logical change per commit.\n\nDestructive commands, non-negotiable:\n  - NEVER git clean / reset --hard / checkout -f / bare or -u git stash on files you did not create.\n  - Need a clean tree? `git worktree add --detach /tmp/<name> origin/main`. Costs nothing.\n  - Must stash? Scope to your own explicit paths.\n  - Announce before any history rewrite. Default: do not.\n\nAnnouncing:\n  - One-line mail on every push to main: repo + sha + what.\n\nAcceptance criteria:\nProtocol is written into a file the sessions actually read (CONTRIBUTING or AGENTS.md in each repo, or the shared skills dir), all sessions have acked it, and the outstanding shared-tree state above is either landed or explicitly abandoned by its owner.","status":"in_progress","priority":1,"issue_type":"task","assignee":"AzureOrchid","created_at":"2026-07-25T10:07:31.876220Z","created_by":"aditya","updated_at":"2026-07-25T10:13:46.855648Z","source_repo":"ZeroStack","source_repo_path":"ZeroStack","compaction_level":0,"original_size":0,"comments":[{"id":1000013,"issue_id":"zerostack-xyk","author":"AzureOrchid","text":"Protocol from 1f90634 read and acked. Adding the missing half rather than just agreeing with it.\n\nWHAT 1f90634 DOES NOT COVER\nThe written protocol governs BEAD claiming and DESTRUCTIVE GIT. Neither addresses the mechanism that actually produces collisions here: two agents holding two different, correctly claimed beads that both need the same file. Bead claiming structurally cannot prevent that, because beads name work items while collisions happen over paths. The acceptance criteria say \"all sessions have acked it\", but acking a protocol with that hole still leaves us colliding.\n\nConcrete instance from today: I was blocked on TokenZero crates/tokenzero-core/src/tokens.rs for zerostack-spz part 1, and there was no way to discover whether another session was inside it. The only signal available was 78 dirty files, which tells you someone is somewhere but not where.\n\nDELIVERED, ZeroStack e3ec16d\nscripts/agent_lock.py, advisory file locks shared across all four repos.\n  agent_lock.py list\n  agent_lock.py check  <Repo>/<path> --who <name>    exit 1 if held by a peer\n  agent_lock.py claim  <Repo>/<path> --who <name> --why \"<bead>\"\n  agent_lock.py release <Repo>/<path> --who <name>   or --all-mine\nKeys are Repo/relative/path so one hub namespace covers ZeroStack, TokenZero, FSZero, GraphZero. State is .agent-locks.json at the hub root, gitignored, so no tracked file changes and no agent names are published. AGENT_NAME env var replaces --who.\n\nDESIGN CHOICES, stated because they are the load-bearing ones\nAdvisory, not mandatory. It cannot stop a write and does not try. These trees hold tens of thousands of files and nobody will route every edit through a wrapper; a lock that pretended to be enforcing would be a lie that invites reliance on a guarantee it cannot make. The value is making intent visible BEFORE an edit.\nLocks expire, default 2h, --ttl tunable. A crashed session must not wedge a file forever. Stale locks print [STALE, breakable] and can be taken without --force, with a note naming the previous holder.\nFails open, not closed. A corrupt .agent-locks.json is ignored with a warning. Blocking real work because an advisory coordination file got mangled would be worse than the race it guards.\n\nVERIFIED, ten behaviours end to end\nclaim; peer check exits 1; own check exits 0; peer claim refused printing holder and reason; free path exits 0; stale takeover under --ttl 0; release by non-owner refused; release by owner succeeds; corrupt file degrades to \"no active locks\"; .agent-locks.json confirmed gitignored.\n\nDOCUMENTED\nAGENTS.md now carries it beside the git protocol, including the social rule that matters more than the tool: if a file is held, MAIL THE HOLDER rather than waiting or barging, since usually two agents are in different regions of a file and splitting is cheaper than either blocking.\n\nREMAINING FOR THE ACCEPTANCE CRITERIA, not mine to close\n\"All sessions have acked\" and \"outstanding shared-tree state landed or explicitly abandoned by its owner\" both need the other sessions. Current state:\n  TokenZero  harness-no-host-paths, 2 ahead of origin/main, 78 dirty, 20 stashes, 2 worktrees\n  FSZero     main, 16 dirty, 21 stashes, 12 worktrees\n  GraphZero  main, 86 dirty,  6 stashes,  3 worktrees\n  ZeroStack  main, clean\nThose two unpushed TokenZero commits (eb71f0d, 4011cc5) are the protocol's own failure case: parked work is invisible and invites duplication. I have mailed asking for them to be pushed and for each session to declare which of the 47 stashes and 17 worktrees are live. I will not touch any of it unilaterally, since deciding what is dead for another session is exactly the judgement that caused the near-miss recorded in this bead.\n\nLeaving this bead OPEN pending those acks and that cleanup. The protocol and the tooling are done; the human coordination half is not.ZEOF\nwc -c /tmp/az/xyk.txt\n","created_at":"2026-07-25T10:13:46Z"}]}
{"id":"zerostack-yoe","title":"wave 2: zero-codemode — shared rquickjs runtime, containment, plan/step machinery","description":"Largest remaining pool: TZ mcp/codemode/* (~8K: exec 3803, journal 969, containment 803, store 702, parser 462), FZ codemode/* (~2.5K: js 732, host 603, connector 466), GZ codemode/* (~3.5K: quickjs 638, steps 574, executor 517, response 512, host_permit 495). Shared crate owns: rquickjs engine setup (unify TZ 0.10 -> 0.12 to match FZ), sandbox/containment policy, plan parsing, step journaling, host-permit model, response envelopes. Engines register only their own host functions/connectors (zero.token.*, zero.fs.*, zero.graph.*). Bisectable: rquickjs bump lands as its own commit inside the wave.","status":"open","priority":1,"issue_type":"task","owner":"adityavgcode@gmail.com","created_at":"2026-07-24T09:20:38Z","created_by":"AdityaVG13","updated_at":"2026-07-24T09:20:38Z","source_repo":".","compaction_level":0,"original_size":0}
