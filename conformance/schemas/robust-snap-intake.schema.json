{
  "$defs": {
    "digest": {
      "pattern": "^[0-9a-f]{64}$",
      "type": "string"
    }
  },
  "$id": "https://zerostack.dev/schemas/racc-r/robust-snap-intake-v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "dominance_scope_digest": {
      "$ref": "#/$defs/digest"
    },
    "fiber_completeness_receipt_digest": {
      "$ref": "#/$defs/digest"
    },
    "fiber_representation": {
      "enum": [
        "finite_exact",
        "conservative_superset",
        "unknown"
      ]
    },
    "protected_use_scope_digest": {
      "$ref": "#/$defs/digest"
    },
    "schema_version": {
      "const": "zerostack.robust_snap.intake_claim"
    },
    "snap_certificate_digest": {
      "$ref": "#/$defs/digest"
    },
    "verifier_identity_digest": {
      "$ref": "#/$defs/digest"
    }
  },
  "required": [
    "schema_version",
    "snap_certificate_digest",
    "fiber_representation",
    "fiber_completeness_receipt_digest",
    "protected_use_scope_digest",
    "dominance_scope_digest",
    "verifier_identity_digest"
  ],
  "title": "RACC-R proof-carrying Robust Snap intake claim V1",
  "type": "object"
}
