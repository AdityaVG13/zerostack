# Phase 12 — zs / MCP leak

**Bead:** `zerostack-gauntlet-p12-honesty-engines-qaa6`

`scripts/zs` should be a raw-worker / CodeMode argv wrapper: no MCP JSON-RPC, no `form:js` plan, no engine catalog fallback. Keep the `zs` name.

Finding beads: `p11i2-zs-mcp-plan-catalog-jwa7`, `p11i1-mcp-cancel-detach-knim`, `p11i1-node-cancel-late-result-70u2`, `p1-dual-cli-zs-zsx-so07`.

Negative space (raw-worker): no planner, no javascript_runtime, no mcp_catalog, no nested_codemode.
