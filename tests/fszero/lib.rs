//! Workspace member for TokenZero-shaped integration tests.
//!
//! Product crate `fs-zero` owns `[[test]]` paths under crate-named subdirs
//! (`core/`, `cli/`, `mcp/`, `codemode/`). Ship-gated binaries can land here
//! the same way `tokenzero-ship-tests` owns `tests/ship/`.
