[package]
name = "graphzero-ship-tests"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

# Not a workspace member. Live integration targets are declared by the
# domain crates. The missing [lib] path was removed rather than replaced
# with an empty crate root.
