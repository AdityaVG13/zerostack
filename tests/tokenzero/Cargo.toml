[package]
name = "tokenzero-ship-tests"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
serde_json.workspace = true
tempfile.workspace = true

# Not a workspace member. Ship integration tests were pruned in d8c0844;
# missing test targets were deleted in Phase 1 rather than left compile-broken.
