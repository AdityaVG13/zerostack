[package]
name = "zerostack-test-support"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Shared test scaffolding for ZeroStack and the three engine repos"

[dependencies]
blake3.workspace = true
sha2.workspace = true
zero-abi = { path = "../../crates/zero-abi" }
