[package]
name = "zero-codemode"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Pure-safe generic CodeMode host foundation"

[features]
default = []
quickjs = ["dep:rquickjs"]

[[bin]]
name = "zerostack-codemode-host"
path = "src/bin/zerostack-codemode-host.rs"
required-features = ["quickjs"]

[dependencies]
serde.workspace = true
serde_json.workspace = true
rquickjs = { version = "0.12", optional = true, default-features = false, features = ["full", "macro"] }
