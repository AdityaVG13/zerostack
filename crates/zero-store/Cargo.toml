[package]
name = "zero-store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Canonical ZeroRef v1 content-addressed store layout and publish protocol"

[dependencies]
sha2 = { workspace = true }

[dev-dependencies]
tempfile = "3"
serde_json = { workspace = true }
