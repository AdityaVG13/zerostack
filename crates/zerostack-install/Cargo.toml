[package]
name = "zerostack-install"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Install engine for ZeroStack-family harness packages: plan/apply/rollback, doctor, agent detection, archive integrity"

[dependencies]
flate2 = "1"
fs4 = "1.1.0"
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
toml = "0.9"
zero-store = { path = "../zero-store" }

[dev-dependencies]
tempfile = "3"
