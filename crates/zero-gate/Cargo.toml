[package]
name = "zero-gate"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Proof-carrying deterministic decision gate for ZeroStack"

[dependencies]
sha2 = { workspace = true }
serde = { workspace = true }
zero-abi = { path = "../zero-abi" }
zero-cert = { path = "../zero-cert" }

[dev-dependencies]
criterion = "0.5"
proptest = "1"
stats_alloc = "0.1"

[[bench]]
name = "decide"
harness = false
