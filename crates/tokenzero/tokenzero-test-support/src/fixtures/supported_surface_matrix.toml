# TokenZero supported surface matrix (Phase 2 greenfield; Pass 10 re-audit).
# Status vocabulary: supported | partial | excluded | missing
# Partial NEVER rounds up to supported.
# Axis 11 strict-mode is MISSING; do not invent a flag.
# 14 authority axes + never-worse workspace gate + MCP/CLI honesty + excluded honesty pins.

schema_version = "gauntlet.supported_surface_matrix"

[meta]
project_class = "Greenfield-Rust-class"
reference_name = "tokenzero-spec"
reference_version = "HEAD-fb73416"
subject_git_sha = "65ce648"
generated_at_utc = "2026-08-24T10:24:00Z"
revision = 3
identity_subject_pin = "GauntletSubject::TokenZero@862e3e682cb8aee0e150c1cb0b116cb2e23a44e2"
identity_pin_note = "Gauntlet Subject identity stays prior-commit 862e3e6 (Self-oracle baseline). subject_git_sha is the Pass 10 re-audit HEAD (Phase 6 crash windows). Do not conflate the two."
status_vocabulary = ["supported", "partial", "excluded", "missing"]
partial_never_rounds_up = true

[categories.tokenizer-identity]
weight = 0.10
description = "Exact tokenizer identity vs labeled estimator. Never present estimator as Q99."

[categories.pages-capsules]
weight = 0.10
description = "Token pages, capsules, MAX_* contracts, expand round-trip."

[categories.decision-views]
weight = 0.08
description = "Decision Views. Core-complete, surface-incomplete. Partial never rounds up."

[categories.prefix-geometry]
weight = 0.08
description = "Stable-prefix geometry."

[categories.eligibility-hit]
weight = 0.10
description = "Provider eligibility vs reported hit. Never from LCP."

[categories.reasoning-state]
weight = 0.07
description = "Opaque reasoning-state transport. Core types; surfaces incomplete."

[categories.headroom]
weight = 0.06
description = "DecisionViewHeadroomPlan. Engine call-limit is a different concept."

[categories.shell-witnesses]
weight = 0.07
description = "Shell witnesses. Temporal interleaving not claimed."

[categories.novelty-continuation]
weight = 0.08
description = "Output novelty and continuation classes. Distinct from entity_novelty."

[categories.never-worse-perf]
weight = 0.10
description = "Never-worse-than-raw + workspace gate. Unit estimator:bytes-ceil-div4/v1. Not Q99. million-line-nav fails closed."

[categories.mcp-cli-honesty]
weight = 0.08
description = "MCP/CLI honesty, CLI .golden, skip≠green, Subject≠Oracle, TokenEngine z.* kernel orifices. Strict-mode MISSING (do not invent a flag)."

[categories.durability-recovery]
weight = 0.08
description = "Fail loud, hub-only imports, dual-store. FSZero/GraphZero crate imports excluded."

# Sum of category weights = 1.00 (global). Per-category feature weights also sum to 1.00.

[[features]]
id = "F-TZ-001"
title = "Exact tokenizer identity (provider-locked; never conflate estimator with exact/Q99)"
category = "tokenizer-identity"
weight = 0.55
status = "partial"
authority_axis = 1
partial_rationale = "tokenzero-core ExactTokenizerIdentity exists; tokens.rs TokenizerMetadata.approximate=true; Pulse default tokenizer_id=estimator:tokenzero-core. Goldens distinguish exact/approximate/unverified=null. Dual stack; do not treat estimator count_tokens as Q99 or ExactTokenizerIdentity."
evidence = "phase1_unified_recon.md axis 1; tests/engine/fixtures/provider-tokenizer-goldens.json; tests/core/fixtures/one-token-atoms.json"

[[features]]
id = "F-TZ-001-EST"
title = "Estimator vs exact vs tiktoken count_class labeling (Pulse tokenizer_id grammar)"
category = "tokenizer-identity"
weight = 0.45
status = "supported"
evidence = "Pulse tokenizer_id grammar estimator:<slug> | tiktoken:<encoding> | provider/model@<64hex>. tiktoken: is bundled BPE (certified for that vocab), not ExactTokenizerIdentity. Kernel measure gpt-4o emits tiktoken:o200k_base and Pulse accepts it. Goldens encode unverified=null. Bare Q99/exact/MCP registry ids still fail."

[[features]]
id = "F-TZ-002"
title = "Token pages / capsules (TokenPage, ModelCapsule, Capsule)"
category = "pages-capsules"
weight = 0.70
status = "supported"
authority_axis = 2
evidence = "tokenzero-core model_artifacts.rs MAX_*; engine_read tests; census identity artifact"

[[features]]
id = "F-TZ-002-RT"
title = "Pages/capsules expand round-trip (TokenPage MAX_* + expand; not dual-store fragment fuzz)"
category = "pages-capsules"
weight = 0.30
status = "partial"
partial_rationale = "TokenPage constructors, MAX_* limits, and expand() exist. Dual-store fragment expand fuzz (expand_fragment_differential.rs) is SPEC-TZ-RT-002, not TokenPage/ModelCapsule RT. ModelCapsule encode/decode still has no live driver after d8c0844. Partial MUST NOT round up."
evidence = "tests/unit/tokenzero-core/model_artifact_limits.rs; crates/tokenzero-core/src/model_artifacts.rs MAX_*"

[[features]]
id = "F-TZ-003"
title = "Decision Views (core types + tests)"
category = "decision-views"
weight = 1.00
status = "partial"
authority_axis = 3
partial_rationale = "Present in tokenzero-core + tests (stable-before-volatile, token-aligned prefix, capsule source-root mismatch fail-loud). Missing engine dispatch, CLI, and MCP. Partial MUST NOT round up."
evidence = "phase1_unified_recon.md axis 3"

[[features]]
id = "F-TZ-004"
title = "Stable-prefix geometry (StablePrefixGeometry + PrefixStabilityGuard)"
category = "prefix-geometry"
weight = 1.00
status = "supported"
authority_axis = 4
evidence = "tokenzero-core StablePrefixGeometry; recovery PrefixStabilityGuard 5/4 floor; tests/unit/tokenzero-recovery/prefix_stability_floor.rs"

[[features]]
id = "F-TZ-005"
title = "Provider eligibility vs reported hit (never conflate; never from LCP)"
category = "eligibility-hit"
weight = 1.00
status = "supported"
authority_axis = 5
evidence = "tests/engine/fixtures/prefix-probe-replay.json; tests/unit/tokenzero-engine/prefix_probe_eligibility.rs: raw_retained LCP full + hit_declared_by_provider=false"

[[features]]
id = "F-TZ-006"
title = "Opaque reasoning-state transport (OpaqueReasoningStateEnvelope)"
category = "reasoning-state"
weight = 1.00
status = "partial"
authority_axis = 6
partial_rationale = "Types+tests in core; not on engine/MCP/CLI surfaces. Partial MUST NOT round up."
evidence = "phase1_unified_recon.md axis 6"

[[features]]
id = "F-TZ-007"
title = "Headroom (DecisionViewHeadroomPlan)"
category = "headroom"
weight = 1.00
status = "partial"
authority_axis = 7
partial_rationale = "Hub ReasoningContract + core plan types. Engine codemode_wire 'headroom' is call-limit, not this plan. Do not score the call-limit as DecisionViewHeadroomPlan. Partial MUST NOT round up."
evidence = "phase1_unified_recon.md axis 7"

[[features]]
id = "F-TZ-008"
title = "Shell witnesses (engine_shell private; temporal interleaving not claimed)"
category = "shell-witnesses"
weight = 1.00
status = "partial"
authority_axis = 8
partial_rationale = "Exact stream witness tests exist; engine_shell is not crate-public; combined_witness_temporal_interleaving: not_claimed. Partial MUST NOT round up."
evidence = "phase1_unified_recon.md axis 8"

[[features]]
id = "F-TZ-009"
title = "Output novelty (OutputNoveltyReceipt)"
category = "novelty-continuation"
weight = 0.50
status = "partial"
authority_axis = 9
partial_rationale = "Core+tests; unused by engine/CLI/MCP. Distinct from recovery entity_novelty. Partial MUST NOT round up."
evidence = "phase1_unified_recon.md axis 9"

[[features]]
id = "F-TZ-010"
title = "Continuation classes (ExactNeutral / ScopedCertificate / Empirical / Unavailable)"
category = "novelty-continuation"
weight = 0.50
status = "partial"
authority_axis = 10
partial_rationale = "Classes preserved and tested; not dispatched on engine/CLI/MCP. Partial MUST NOT round up."
evidence = "phase1_unified_recon.md axis 10"

[[features]]
id = "F-TZ-012"
title = "Never-worse-than-raw product promise (same-unit; estimator labeled)"
category = "never-worse-perf"
weight = 0.40
status = "partial"
authority_axis = 12
partial_rationale = "Gate present and honest in benchmarks/; no crate FeatureId owner. Product units remain estimator. Million-line-nav currently fails closed. Partial MUST NOT round up."
evidence = "phase1_unified_recon.md axis 12; docs/benchmarks.md"

[[features]]
id = "F-TZ-015"
title = "Never-worse workspace gate (estimator:bytes-ceil-div4/v1; not Q99)"
category = "never-worse-perf"
weight = 0.40
status = "partial"
partial_rationale = "benchmarks/never_worse_gate.py schema never-worse/v1 rejects Q99 in unit_id/suite; harness.py refuses empty-candidate/error-payload as denominator. scripts/never_worse_gate.py is a legacy duplicate (link, do not fork). million-line-nav fails closed. Workspace gate, not a Rust crate owner."
evidence = "census notes; competitor-bakeoff.sh captured-stdout-bytes/v1 + estimator:bytes-ceil-div4/v1; million-line-nav.sh visible-payload-bytes/v1"

[[features]]
id = "F-TZ-017"
title = "Q99 as TokenZero product unit"
category = "never-worse-perf"
weight = 0.20
status = "excluded"
exclusion_rationale = "Never-worse is estimator:bytes-ceil-div4/v1 (or the named suite unit). Q99 is not a TokenZero product unit. AGENTS.md 'Q99 labeled' means any Q99 figure must stay labeled, not that Q99 is the gate unit."
retry_condition_predicate = "Reconsider only if TokenZero grows a separately named Q99 product unit with its own denominator, countermetric, and gate that cannot be confused with estimator:bytes-ceil-div4/v1."

[[features]]
id = "F-TZ-016"
title = "MCP/CLI honesty (CLI .golden, skip≠green, no insta self-compare)"
category = "mcp-cli-honesty"
weight = 0.25
status = "partial"
partial_rationale = "8 CLI .golden files remain as Self-oracle corpus; the golden_outputs [[test]] was deleted in d8c0844 so they are not live-compared. Ship tests skip without env (honest skip≠green). Gauntlet Subject/Oracle discriminator exists in tokenzero-test-support (do not claim it is missing). capabilities.json now names TokenEngine z.* separately (F-TZ-021). Partial MUST NOT round up."
evidence = "tests/cli/golden/cli/ (8 files); tests/unit/tokenzero-test-support/gauntlet_oracle_smoke.rs; tests/cli/cli_help_contract.rs kernel_orifices + clap-vs-capabilities; no cargo-insta in Cargo.lock"

[[features]]
id = "F-TZ-021"
title = "TokenEngine kernel orifices (z.measure / z.project / z.compress / z.expand)"
category = "mcp-cli-honesty"
weight = 0.15
status = "supported"
evidence = "zero_abi::TokenEngine on ZeroTokenEngine: measure, certify, project, compress, expand. capabilities.kernel_orifices declares those methods and explicitly lists z.read/z.find/z.run/zero.read as not TokenEngine. Dotted zero.* CodeMode bindings stay noncanonical_v6_compat."

[[features]]
id = "F-TZ-011"
title = "Strict mode never lowers model/effort/tools/output"
category = "mcp-cli-honesty"
weight = 0.25
status = "missing"
authority_axis = 11
missing_rationale = "rg found no strict-mode type or flag. Closest hit is strictly-cheaper ref rewrite (tokens, not model/effort/tools/output). Do not invent a flag in this gauntlet. Axis 11 is MISSING."
retry_condition_predicate = "Retry only after an operator-named strict-mode surface exists in TokenZero (type/flag/CLI/MCP) that can lower or refuse to lower model, effort, tools, or output. Do not invent that surface from the gauntlet workspace."

[[features]]
id = "F-TZ-018"
title = "MCP EngineIdentity::TokenZero used as gauntlet Subject/Oracle"
category = "mcp-cli-honesty"
weight = 0.20
status = "excluded"
exclusion_rationale = "EngineIdentity::TokenZero / RegistryEngine::TokenZero in tokenzero-mcp-compat is MCP registry identity from zero_abi. It is NOT the gauntlet Subject/Oracle discriminator. Using it would be self-comparison."
retry_condition_predicate = "Do not retry. Gauntlet identities are GauntletSubject::TokenZero@<sha> and per-mode GauntletOracle::* strings in spec_version_contract.toml [mixed_oracles]."

[[features]]
id = "F-TZ-019"
title = "cargo-insta / .snap golden mechanism"
category = "mcp-cli-honesty"
weight = 0.15
status = "excluded"
exclusion_rationale = "No insta .snap files (0). cargo-insta absent from Cargo.lock. CLI goldens are custom .golden under tests/cli/golden/. Pin those, do not introduce insta."
retry_condition_predicate = "Reconsider only if TokenZero adopts cargo-insta as a first-class blessed snapshot tool with a bead and bless_policy=manual. Until then pin .golden files."

[[features]]
id = "F-TZ-013"
title = "Fail loud; no silent deletes"
category = "durability-recovery"
weight = 0.40
status = "partial"
authority_axis = 13
partial_rationale = "Fail-loud tests exist; also let _ = fs::remove_file on tmp/error paths; dry_run guards on prune. Partial MUST NOT round up."
evidence = "phase1_unified_recon.md axis 13"

[[features]]
id = "F-TZ-014"
title = "Hub contracts only; no FSZero/GraphZero crate imports"
category = "durability-recovery"
weight = 0.40
status = "supported"
authority_axis = 14
evidence = "workspace Cargo.toml: hub and engine crates resolve through monorepo-local workspace dependencies. Engine crates do not import sibling engines. URI/comment coupling is not an import violation. Driver: tests/tokenzero/unit/tokenzero-test-support/gauntlet_oracle_smoke.rs"

[[features]]
id = "F-TZ-020"
title = "FSZero/GraphZero crate imports from TokenZero product code"
category = "durability-recovery"
weight = 0.20
status = "excluded"
exclusion_rationale = "Forbidden by AGENTS.md. Composition is the hub's job. fz:// and gz:// expand aliases are hub URI compatibility, not crate imports."
retry_condition_predicate = "Do not retry. If a Cargo dependency on FSZero or GraphZero appears, that is a law violation, not a gauntlet feature to add."

[coverage_debt]
supported_count = 6
partial_count = 12
missing_count = 1
excluded_count = 4
na_count = 0
feature_count = 23
authority_axis_count = 14
# local = sum(per-category feature weights); 12 categories * 1.0 = 12.0
# missing+excluded local: F-TZ-017 0.20 + F-TZ-011 0.25 + F-TZ-018 0.20 + F-TZ-019 0.15 + F-TZ-020 0.20 = 1.00
local_missing_plus_excluded_weight = "1.00"
local_all_weight = "12.00"
local_debt_fraction = "0.083333"
global_weighted_missing_plus_excluded = "0.0840"
notes = "Excluded still counts as coverage debt for a strict-100% claim. Partial never rounds up. Pass 10: Pulse tiktoken: grammar + TokenEngine z.* kernel_orifices (F-TZ-021). mcp-cli-honesty rebalanced 0.25+0.15+0.25+0.20+0.15."

